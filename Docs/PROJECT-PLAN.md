# SetterCheckerNew 设计与进度

## 一、最终目标

SetterCheckerNew 只服务于 `D:/KiHan/Packages/khengine`。

它读取 khengine 当前真正使用的 Unity 编译参数，检查所有相关源码和编译后的 C# 文件，最后回答：

1. 哪些可报告函数的真实行为是 Setter。
2. 哪些可报告函数的真实行为是 Getter。
3. 分析结论与源码现有标签有哪些差异。
4. 哪些人工豁免需要复查，哪些裸 `[NoLogTrack]` Setter 缺少 Reason。

工具生成文本报告、内容一致的 JSON 报告和标签修改预览，不自动修改 KiHan。

## 二、已经确认的判断规则

真实行为只分 Getter 与 Setter。以下行为属于 Setter：

- 修改当前对象、传入对象、对象图、静态数据、集合、数组、事件或 `ref/out` 数据。
- 调用函数后实际修改了调用者已有的对象、参数或静态数据；记录被调函数修改的是接收对象还是第几个参数，再映射回调用者，不能无条件复制被调函数的 Setter 结论。
- 创建 khengine 业务对象并将它返回、保存、注册或传给外部。

只修改函数内部创建且没有离开函数的临时对象，不属于 Setter。

只有函数自身没有外部修改，并且全部合法调用目标的实际影响都闭合、没有造成外部修改，函数才是 Getter。不能闭合时停止分析并报告准确位置，禁止猜测、保守判定或继续生成不完整结论。

`ShouldTrack`、`NoLogTrack` 和 `NLTClass` 是第六模块结合真实行为与源码标签后得出的最终日志决定，不是函数真实行为的别名。`[LogTrack]` 只强制最终决定为 `ShouldTrack`，不能把真实 Getter 改写成 Setter。

## 三、现有标签规则

- 方法级 `[NoLogTrack(Reason)]`、裸 `[NoLogTrack]` 和类级 `[NoLogTrack]` 都先作为可信人工豁免，最终不追踪。
- 工具仍独立检查所有豁免函数的真实行为。
- 类级豁免下的可报告函数标记为 `NLTClass`。
- 带 Reason 的 Setter 和类级豁免 Setter 进入“可信豁免复查”，不报警。
- 只有裸 `[NoLogTrack]` 被独立分析为 Setter 时，告警“缺少 Reason”。
- 是否有 Reason 读取当前标签构造函数真正收到的原因字符串；不能用“括号里有参数”代替。当前 khengine 还存在只有 `NoLogTrackFlag` 数值的写法，它没有提供 Reason。
- 该告警传到所有能调用它的上层可报告函数；每个函数只列一次最短调用过程。
- 没有 NLT、但实际是 Getter 的函数进入补标建议。
- 同一函数或其所属类同时出现互相冲突的 NLT 与 `[LogTrack]` 时，正式分析失败并指出冲突位置，不猜测哪一个优先。

现有标签不作为语义分析的答案，只影响最后的日志决定和差异分类。

## 四、统计范围

“可报告函数”指进入最终数量统计和标签差异的函数，包括：

- 普通命名函数。
- 运算符和类型转换函数。
- 属性 setter。
- 自定义事件 add/remove。
- 类级 NLT 下原本可报告的上述函数。

构造函数、析构函数、属性 getter、接口或抽象声明、局部函数、匿名函数、编译器生成函数、`getInstance` 和 `BaseProxy_*` 不进入标签统计，但仍参与调用分析。

## 五、七个模块

生产代码按以下七个模块组织，一个模块一份主文件。主文件可以包含本模块紧密相关的小类型，不再按类拆文件。

### 1. `MaterialLoader.cs`：找齐材料

- 找到 `khengine.runtime.asmdef` 当前唯一的 Unity 编译参数文件。
- 读取实际源码、条件编译标记、分析器引用、源码依赖和外部编译文件。
- 递归读取依赖的 Unity 源码程序集，但只有 khengine 进入最终统计。
- 保留 Unity 响应文件中的完整编译选项、分析器和源码生成器；生成的源码与普通源码一起进入材料。
- 保留响应文件的源码顺序，把当前源码编译到内存，不读取或覆盖磁盘上的 Unity 输出；编译耗时单独列出并计入总时间。
- 源码依赖按依赖顺序分批编译，互不依赖的程序集并行；上层使用本轮依赖的内存产物，不能用同名旧参考文件的成员表代替当前源码。源码新增或删除成员均进入本轮编译验证。
- 参考文件以及类型转交目标映射到同一 Unity/.NET 环境中的真实承载文件；普通间接依赖先进入候选表，实际命中时按完整程序集身份载入。
- 先按当前 Unity 运行目录与已证明的程序集身份规则选择真实载体，参考文件即便只有类型转交记录也不能绕过这一步。参考文件继续供编译器使用，不与已选定的真实载体并列成两个运行分支。若真实载体自身继续转交，则沿它的真实记录查找。
- 只有完全没有同名运行载体的纯参考门面才保留为查找类型终点的别名材料，不贡献函数实现。普通参考文件缺少实现、身份不符或有多个实际载体仍明确失败；不能从编译参考补出真实运行库没有的行为。当前 Unity 编辑器的运行目录由实际编译配置与编辑器日志核实。
- 转交表中能唯一定位的载体继续进入候选范围；尚未使用且没有载体的转交记录保留，实际调用、继承或接口命中该类型时才强制查齐。不能因为参考门面还列有从未用到的类型，就读取其他平台或无关工具目录来补齐。多个合法载体、已登记的循环或身份冲突仍明确失败。
- 找不到、找到多份或身份不一致时直接失败并列出证据。

### 2. `MethodCatalog.cs`：建立函数总表

- 源码内存产物与外部托管文件共用一套类型、参数、泛型和继承事实读取。Roslyn 与 Cecil 用现成的标准文档成员编号配对；源码只回贴位置、显示名、标签和统计范围，不能猜配对。
- 为每个源码命名函数保留逻辑身份；外部及编译器生成函数以“真实文件 + 元数据标记”天然唯一，在实际调用或候选类型需要时读取并在本次运行内保存，避免启动时创建几十万个无关对象。
- 记录类、函数、参数、文件、行号、标签和是否进入最终统计。
- 预先建立全部类型的继承与接口实现索引，以及源码函数的重写与成员名称索引；外部候选类型一旦被命中，就从同一真实文件读取该类型的函数，函数自身携带参数、重写目标和实现目标供后续模块使用。

### 3. `BehaviorReader.cs`：读取函数行为

- 源码先由 C# 编译器转换成当前内存中的函数内容；源码位置、标签和可报告函数身份仍由源码目录保留。
- 当前源码的内存编译结果和真实托管动态链接库共用一套函数指令读取，不保留第二套源码行为分支或回退路径。
- 统一记录字段写入、属性写入、数组或集合写入、事件修改、`ref/out` 修改、调用、对象创建、返回、保存、注册、传出和对象来源。
- 每次普通变量读取都保留独立快照、所在基本块和执行顺序；赋值、调用、写入、返回使用同一套位置。变量的稳定存储身份只用于取地址、引用传参和闭包捕获。保留编译器给出的分支、异常处理和最终释放关系，不能只记源码行号或合并所有历史赋值。
- 这一模块只描述事实，不直接给出 Getter、Setter 或标签结论。
- 正式运行先读取 khengine 可报告函数，依赖函数体在模块 4 找到实际目标后补读；依赖类型和函数目录仍预先建立。
- 迭代器按编译器实际生成的创建函数与遍历函数分别读取；创建时不执行遍历函数，不手写第二套状态机或捕获规则。编译器生成函数参与调用分析，但不进入标签统计。

### 4. `CallTargetResolver.cs`：找齐调用目标

- 依据第三模块保留的执行关系，统一计算每个使用位置真正能到达的值来源；委托、反射与第五模块共用这份结果，不各自重复实现一套变量分析。
- 普通调用连接到明确函数。
- 接口调用连接到全部合法实现。
- 可重写函数连接到全部合法重写。
- 委托连接到全部可能绑定的函数。
- 事件回调与委托使用同一套绑定目标规则。
- 反射按类型、名称和参数解析字段、属性、函数与构造函数。
- `GetMethod("Foo")` 只匹配合法的 `Foo`；名称来自参数时追踪全部调用点传值。
- 反射名称允许由外部任意传入时，检查当前代码闭包内所有类型和签名能够匹配的成员。
- `FieldInfo.SetValue` 和 `PropertyInfo.SetValue` 产生普通写入关系，不硬编码最终结论。
- 每找到尚未读取的目标，就交给模块 3 补读，再继续解析它的调用；直到没有新目标。开发仍按模块顺序验收，运行时这两个模块共同完成调用链。
- 普通调用迭代器函数只创建可遍历对象，只有实际遍历才连接其延迟函数体；不得在创建时直接传播函数体中的写入。

本模块按以下顺序实现和验证，不同时铺开多套算法：

1. 先准确接上普通调用和构造函数，复用函数总表中的身份与类型信息；每轮集中补读新命中的函数，已经读过的不重读。
2. 接上接口与可重写函数；候选表按真实继承、实现关系建立，不按函数名相似程度匹配。
3. 利用第三模块的执行顺序，计算一次变量在使用位置可能来自哪里。地址仍连接原变量，普通读取保留当时的值；这份结果供委托、反射与下一模块共同使用。
4. 接上委托、事件和反射；实际目标与实际接收对象、参数一起保存，不能只列出一个函数名而丢掉调用方式。
5. 没有新目标后固定结果顺序。保留全部参与分析的函数、已读行为、逐调用点的目标、共用值来源及耗时，不提前输出 Setter/Getter。

新写法加入本模块已有的目标读取或值来源处理，不新建插件、规则注册框架或按业务函数定制分支。每一步先做源码和真实 DLL 的对照测试，再用 khengine 检查实际调用；本模块全部审计通过之前不实现第五模块。

2026-09-07 根据用户要求自主决定：按被调用函数实际修改的对象向上传递影响。修改静态数据或调用者已有对象时传播；只修改调用者内部新建且没有传出的对象时不传播。参考 `Runtime/KFL/DSL/KFLCsLuaASTEncodeUtil.cs:135`：内部新建 `StringBuilder` 并调用 `Append`，最后返回字符串，拼接对象本身没有传出。仅这条拼接路径不能因为 `Append` 会修改自身，就判定上层修改了外部状态；其它调用仍独立检查。

### 5. `EffectAnalyzer.cs`：得出真实结论

- 先标出直接修改外部状态的函数。
- 再沿调用关系传播实际修改的对象和参数，逐调用点判断是否影响调用者外部状态。
- 跟踪新对象是否只在函数内部使用，还是被返回、保存、注册或传出。
- 循环调用使用同一轮稳定计算，不重复扫描全部函数。
- 全部关系闭合后，每个函数的真实行为只有 Getter 或 Setter 两种结论。

### 6. `AnnotationEvaluator.cs`：处理标签和差异

- 应用可信 NLT 与 `[LogTrack]` 规则。
- 生成可信豁免复查、裸 NLT 缺少 Reason 告警、上游调用过程和可补标清单。
- 只在这一模块处理标签，不改变前五个模块的真实行为分析。

### 7. `ReportWriter.cs`：生成交付结果

- 输出总函数数、检测结果数量和源码标签数量。
- 按类汇总两类主要差异。
- 输出可信豁免复查、缺少 Reason、失败证据和每阶段耗时。
- 从同一结果生成文本、JSON 与补丁预览，顺序固定。

## 六、代码目录

仓库只保留三个项目：

```text
Src/SetterChecker.Core/
  SetterChecker.cs              总入口、共享数据和七个模块的顺序
  MaterialLoader.cs
  MethodCatalog.cs
  BehaviorReader.cs
  CallTargetResolver.cs
  EffectAnalyzer.cs
  AnnotationEvaluator.cs
  ReportWriter.cs
  SetterChecker.Core.csproj

Src/SetterChecker.Cli/
  Program.cs
  SetterChecker.Cli.csproj

Tests/SetterChecker.Core.Tests/
  MaterialLoaderTests.cs
  MethodCatalogTests.cs
  BehaviorReaderTests.cs
  CallTargetResolverTests.cs
  EffectAnalyzerTests.cs
  AnnotationEvaluatorTests.cs
  ReportWriterTests.cs
  TestProject.cs
  SetterChecker.Core.Tests.csproj
```

不建立空目录、插件系统、依赖注入容器、适配层、一类一文件结构或未来需求占位。生产入口不支持任意普通 `.csproj` 或通用 NuGet 工程；自动化测试仍可建立最小 C# 夹具验证源码与动态链接库的同一写法。`bin/Debug`、`bin/Release`、`obj`、`artifacts` 和 `TestResults` 都是临时生成内容，不属于源码，不提交。

手写生产代码以 3000–5000 行为优先目标。用户于 2026-09-07 确认允许必要超出，但 Core 全部手写 C# 必须小于或等于 10,000 个物理行（包含注释、空行，排除 bin/obj）。不能为了压行数牺牲功能和可读性，也不能靠移到 Cli、拆文件或压缩排版绕过上限；必须报告实际行数及超出优先目标部分的用途。

模块 3 首轮实现使手写生产代码增长到约 8,300 行，复杂度审计判定已经偏离目标。模块 2 因此重新打开：改用项目统一版本的 Mono.Cecil 读取托管元数据和指令，删除自行实现的元数据框架、非 khengine 边界样本和重复身份结构。压缩不能删除真实项目需要的分析能力；预计超过 5,000 行时，必须先列出不能省略的部分和当前精确行数。

每个函数的 XML 文档块之前写一行简短中文 `//` 注释，直接说明这个函数的用途；没有 XML 文档时注释紧贴函数声明。公开函数仍按统一编码规范保留必要的 XML 文档，但不写重复的大段说明。

## 七、外部代码与动态调用

- 当前 Unity 编译参数列出的源码和托管动态链接库都必须读取真实内容。
- 只有声明的参考文件必须映射到当前运行环境真实文件后再分析。
- 包装函数通过自己的源码或函数指令自然进入调用关系，不为 KFL、ReflectUtil 或具体业务函数写特例。
- 接口、重写、委托和反射统一遵守“全部合法目标都闭合”的规则。
- `Activator.CreateInstance` 解析传入类型及构造函数，并继续判断新对象是否离开函数。
- 第一轮不使用 LogicData 限制反射目标，不枚举未启用的条件编译组合。
- 如果某个可报告函数的结论依赖原生函数体，正式分析失败并指出调用过程；不影响任何可报告结论的原生边界才进入延期清单。
- 运行时生成新代码或加载当前材料范围外的程序集时直接失败，不提供兜底结果。

## 八、禁止的实现方式

- 禁止按某个业务函数名、类名或报告差异硬编码 Getter、ShouldTrack 或忽略结论。
- 禁止 `Unresolved => ShouldTrack`、`Unresolved => NoLogTrack` 或任何保守兜底。
- 禁止捕获所有异常后继续分析、缺文件时扫描全盘、按相似文件名猜实现。
- 禁止为未出现的输入格式、旧版本、插件能力或跨运行缓存预建抽象。
- 禁止为了“职责纯洁”把一段连续流程拆成大量只转发一次的小类。
- 必需的路径、文件、程序集身份和目标闭合校验不属于防御性编程，必须保留；禁止的是猜测、兜底和额外替代路径。

## 九、多线程和性能

命令行接受 `-j4`、`-j 4` 和 `--jobs 4`。数值是整个工具共用的最大并行工作数，不写死为 4。

默认工作数为 4，任意正整数有效，0 或负数直接拒绝。源码文件、外部文件、函数行为和动态目标等互不依赖的项目使用同一个并行上限。先找齐材料、建立目录，再由模块 3 与模块 4 补齐实际调用链，最后判断行为、处理标签和生成报告。

`-j1` 在取得编译内容前关闭编译器内部并行；同时处理多个源码程序集时也关闭每份编译内容的内部并行，避免外层与内层工作队列叠加突破全局上限。只有当前步骤只处理一份源码程序集时，才允许编译器在 `-j` 大于 1 时使用内部并行。每一步结束后固定排序，因此 `-j1`、`-j2`、`-j4`、`-j8` 必须得到逐字一致的结果。

允许一次运行内使用函数、类型、程序集和调用关系字典，不使用持久化缓存。完整分析目标是 5 秒以内，连同工具编译最低要求是 10 秒以内；每个模块单独计时。

## 十、测试和自动审计

测试只通过模块公开结果或工具总入口验证行为，不检查内部拆了多少函数、调用了几次私有方法。每增加一种能力执行一次“先失败、再实现、再整理”的小循环。

同一个最小函数必须分别从源码和真实托管动态链接库读取，两种来源得到的写入、调用、创建、对象来源和最终真实行为必须一致。

全部代码组织、实现、测试编写与执行由主 Agent 负责；子 Agent 只做只读审计，审计打回的问题由主 Agent 修复，不把实现工作再分派出去。

每个模块必须经过：

1. 用明确预期的小例子覆盖正常情况和当前项目实际边界。
2. 编译、格式检查和全部自动化测试通过。
3. 在 KiHan 中只读查找本模块涉及的真实写法，并建立一一对应的最小测试。
4. 把代码、测试和本文件交给子 Agent 审计。
5. 审计不通过就自动修复并重新送审，直到明确通过；只有审计通过后才提交和推送。在已确认范围内自主判断并记录理由；只有需要额外授权或实质扩大范围时才询问用户。
6. 等待审计期间继续做不依赖审计结论的检查、真实写法整理和后续测试资料准备，不空等；下一模块实现仍在当前模块通过后开始。
7. 通过后检查完整差异，提交并推送可回滚版本。

最终再用 `D:/KiHan/Packages/khengine/Runtime/khengine.runtime.asmdef` 完整运行：

- 每种真实写法都有对应测试。
- 除确认不影响任何可报告函数结论的原生边界外没有无法闭合项。
- 工具结论和源码标签的每项差异都由 AI 复查归因。
- `-j1`、`-j2`、`-j4` 结果一致，并记录性能。

## 十一、执行代办清单

已验收并可回滚的历史进度：**3 / 7 个模块**。更新日期：2026-09-07。本检查点对前三模块做了统一读取重构，独立编译、测试、khengine 读取和限域终审均已通过；第四模块仍在未提交开发区。功能检查点不代表完整工具或性能验收通过。只有测试、khengine 只读验收、子 Agent 审计、修复复审、提交和推送全部完成，才更新完成版本。

| 顺序 | 模块与主文件 | 当前状态 | 通过前还缺什么 |
| --- | --- | --- | --- |
| 1 | 找齐材料 — MaterialLoader.cs | 本检查点复验通过 | 编译耗时仍超过整体性能预算 |
| 2 | 建立函数总表 — MethodCatalog.cs | 本检查点终审通过 | 真实类型转交、接口和重写关系已修复 |
| 3 | 读取函数行为 — BehaviorReader.cs | 本检查点复审通过 | 真实 khengine 前三模块已读完，不代表第四模块调用闭合或最终判断完成 |
| 4 | 找齐调用目标 — CallTargetResolver.cs | 开发中；普通调用、构造类型实参隔离和部分委托通过回归 | 转换后的接收类型、跨函数存储、事件、反射和真实闭包仍未完成 |
| 5 | 得出真实结论 — EffectAnalyzer.cs | 未实现 | 已有对象修改、业务新对象传出、调用传播与循环调用 |
| 6 | 处理标签差异 — AnnotationEvaluator.cs | 未实现 | 人工豁免、缺少 Reason、两类差异及最短调用过程 |
| 7 | 生成交付报告 — ReportWriter.cs | 未实现 | 文本、JSON、标签修改预览和最终 khengine 验收 |

### 当前偏移与收缩任务

- [x] 固定七个模块、每个模块一份主文件，使用 Core、Cli、Tests 三个项目。
- [x] 托管读取已改用现有 Mono.Cecil，已删除生产代码中的手写元数据框架和原始指令字节解码。
- [ ] 尽力接近 3000–5000 行优先目标并满足 Core 10,000 行硬上限：最近一次冻结计数为 9,341 行（材料 1,529、目录 3,191、行为读取 2,345、调用目标 2,176、入口和共享数据 100）。行为读取自身的程序集解析已删除，全部复用目录；后续仍要合并重复引用记录、删除无用索引。第四至第七模块尚未全部完成，当前未超限不代表最终达标。不挪出 Core，不删除功能或压缩排版。
- [x] 增加 Core 全部手写 C# 物理行数自动检查，含注释和空行，排除 bin/obj；超过 10,000 行测试失败。
- [ ] 收缩不必要的函数体扫描：保留全部候选类型与函数目录预扫描，正式读取从 khengine 可报告函数开始，沿实际调用补充依赖函数体；目标解析把新命中的函数交回行为读取。仅登记某依赖不等于它的每个函数体都必须分析。
- [x] 删除 1470 行已无测试调用的旧手写元数据夹具及其无用引用；真实写法与正常源码/托管一致性测试保留。
- [x] 删除每个函数重复构造的值编号字典，直接使用已经按连续编号排列的值列表；删除没有可靠参数属性来源的重复调用参数标记，统一读取真实函数定义。
- [x] 测量统一读取路线：原环境全部源码仅编译需 13.243 秒，曾因此保留两套读取。新对照实验表明工具运行设置对耗时影响显著，同输入根编译由 11.354 秒降至 5.673 秒。按用户最新 Core 硬上限自主选择统一内存编译读取；5–10 秒目标不取消，完整性能仍须验证，不能隐藏编译时间。
- [x] 删除旧源码行为读取桥及其无用途转发、不会再产生的旧枚举和重复派生缓存；没有删除当前需要的行为或测试。后续仍须继续压缩真实重复。
- [x] 源码内存产物与外部 DLL 统一建立结构目录，源码只回贴位置、标签和统计范围；使用现有库的标准成员编号配对，删除重复的参数、泛型及继承事实读取。析构函数排除与嵌套泛型配对已补回归并获限域 PASS。
- [x] 使用现有 Cecil 短指令归一功能，删除手写短参数、局部变量、常量和分支变体处理；原指令位置与异常边界保持，统一回归和独立审计通过。
- [x] 进度清单改为当前状态；已提交的历史审计过程可在 Git 历史查看。

### 按顺序执行

- [x] 模块 1：实现、测试、khengine 验收、子 Agent 审计、提交及推送（`04c9546`）。
- [x] 模块 2：建立函数总表。
  - [x] 保留源码、真实托管文件、泛型、继承、接口、重写、类型转交和统计范围。
  - [x] 间接程序集按完整身份延迟载入，实际缺失时保留准确来源证据。
  - [x] 类型索引改为发布后不修改的完整快照。
  - [x] 缓存只保存直接声明，按当前目录计算函数关系；顺序与并发读取回归通过。
  - [x] 修复类泛型实参误替换函数泛型参数，新增真实泛型重写回归。
  - [x] 精确待提交版本：Release 编译 0 警告、0 错误，全部 26/26 测试和格式检查通过。
  - [x] 子 Agent 对代码与测试终审为 PASS；整体规模问题仍按上方清单继续整改。
  - [x] khengine 在四种并行数量下的函数、类型和统计数量一致。
  - [x] 提交并推送独立模块 2 检查点：`5bab38f`；HTTPS 首次失败后保持证书校验，以 OpenSSL 和 HTTP/1.1 重试成功。
- [x] 模块 3：读取函数行为。
  - [x] 已改用 Mono.Cecil 指令读取；已删除未知源码值的通用兜底。
  - [x] 明确允许的源码操作和托管指令，删除默认遍历未知语法、按指令栈数量猜成普通计算的其余兜底。
  - [x] 修复模式匹配值来源，并核对编译器展开的资源释放、遍历、锁和条件访问。
  - [x] 修复托管错版本依赖、多维数组、最小整数常量、具体值类型接口调用、新对象/数组完整类型和真实 out 参数；相关子 Agent 复审通过。
  - [x] 真实 khengine 的 15,263 个可报告函数已完整读取；修复后的 Debug 与 Release 全套均为 81/81，Release 强制重编译 0 警告、0 错误，完整格式检查通过。
  - [x] 修复终审发现的变量覆盖信息丢失：每次读取保留快照、基本块和执行顺序，稳定槽继续供地址与捕获使用；外部对象被新对象覆盖、反向覆盖、条件分支、引用写回、捕获后改值和托管栈旧值均已成对验证。
  - [x] 捕获绑定显式保存键与外层槽，表达式体通过编译器真实执行图读取；初始化器按声明顺序连接入口，隐式构造函数保留基类调用，自动事件分别读取旧委托并执行附加或移除。
  - [x] 异常、筛选和最终释放区域与 Roslyn 实测一致；不把抛出异常猜成必然离开函数。大正数并行上限也经先失败再修复验证，不新增任意上限。
  - [x] 区分迭代器创建与延迟函数体，测试用特性精确定位托管生成函数并对照；核对源码和托管闭包中捕获 this、参数与局部值后传入注册函数。生产中的创建与遍历调用关系仍留给模块 4。
  - [x] 检查赋值、引用传参、属性、运算符、条件访问、资源释放、遍历、锁、初始化器、迭代器和匿名函数捕获。
  - [x] 同一最小函数分别从源码和托管文件读取，核对调用、写入、对象来源；四种并行数量比较完整公开行为数据，结果一致。
  - [x] 本模块写法已与 khengine 代表性真实代码对应；本模块只读取事实，Getter/Setter 在模块 5 判断，最终“每种写法”复核仍未完成。
  - [x] 全部测试和子 Agent 功能复审通过；整体代码规模仍须继续收缩。
  - [x] 提交和推送本次模块 3 可回滚版本：`fd01fc4`。
- [ ] 模块 4：找齐调用目标。
  - [x] 普通调用按完整身份选择声明，保留实例接收对象、静态调用、构造结果与参数；多层和相互递归只补读一次。
  - [x] 源码和真实 DLL 的基础接口、重写、直接基类调用成对测试通过；同名无关类型不会进入候选，继承同一实现不重复输出。
  - [x] 确定新建类型的接收对象缩窄、基础构造泛型接口实参隔离通过成对测试。
  - [x] 读取快照、分支、循环、嵌套 finally 正常路径和 ref 局部地址改写通过公开结果测试；不覆盖所有异常路径验收。
  - [x] 直接实例、静态、虚函数委托及同一工厂两次返回委托的接收对象隔离通过测试。
  - [x] 修复默认接口、显式构造接口、具体值类型受限调用、协变逆变及精确槽优先；泛型引用/值类型、基础类型接口、枚举与可空值类型约束回归通过。
  - [x] 普通多层调用保留类和函数的具体类型实参，整数和字符串不串用；公开结果同时验证被调函数内部参数能追到所选调用的原始常量。
  - [x] 类型实参不断变化的递归不会无限展开，明确报未闭合；稳定同实参递归仍复用。尚未声称支持所有先变化后稳定的泛型递归。
  - [x] 完成当前回归覆盖的转换接收类型限制：普通转换、连续接口条件、返回值转换、泛型对象/参数、空值混合来源、闭合方差与必不调用路径均通过；独立复审 PASS。尚未把这些用例当作字段及反射目标已闭合。
  - [ ] 完成字段/容器/事件回调来源和递归返回值闭合。
    - [x] 单函数内已确定的新对象字段和常量数组下标：实例回调覆盖、不同对象/下标隔离、同一未改接收变量的写后读通过公开测试。
    - [x] 明确空委托不产生调用目标；空值与真实委托混合时只保留真实目标。直接空值、分支混合、构造后清空字段的源码/DLL 对照通过，调用目标 55/55，独立限域复审 PASS。未知来源仍明确未闭合。
    - [ ] 跨函数保存后调用、不同对象的同一创建/保存函数调用必须隔离；按 `RtTimer` 构造函数保存回调建立的最小纵向测试已通过，源码和 DLL 共八个调用均得到唯一正确目标。已按审计补上“通过对象里的另一个对象改写回调”的真实红绿测试，移除仅检查直接参数的遗漏条件；原生边界不能证明存储影响时准确失败，真实运行时委托构造单独依据通用语义证明。调用组 57/57，独立限域复审 PASS；尚未覆盖没有构造调用祖先的独立入口和递归保存，不代表跨函数存储整体完成。
    - [ ] 审计发现的对象和值分支关联、循环分配对象、字段回指循环、可能别名和静态初值仍未闭合。目前已增加准确失败回归，防止错列目标或进程崩溃；这些失败回归不代表功能完成，必须继续按实际命中情况修复。
  - [ ] 完成实际接收对象来源缩窄、泛型接收类型、委托和事件；当前基础用例通过不代表全部合法目标已闭合。
  - [ ] 反射按类型、名称、签名及参数来源找齐目标；字段写入与属性调用进入普通行为流程。
  - [ ] 从可报告函数出发补齐实际调用的函数体，直到没有新目标；不得按函数名猜结论。
  - [ ] khengine 验收、子 Agent 审计、修复复审、提交和推送。
- [ ] 模块 5：得出 Getter/Setter 结论。
  - [ ] 先写失败测试，再实现已有对象修改、业务新对象传出、调用传播和循环调用。
  - [ ] 无法闭合时给出位置和调用过程，不映射为 Getter 或 Setter。
  - [ ] khengine 验收、子 Agent 审计、修复复审、提交和推送。
- [ ] 模块 6：处理标签差异。
  - [ ] 先写失败测试，再实现类级 NLT、方法 NLT、Reason、LogTrack 与冲突检查。
  - [ ] 裸 NLT 只有真实为 Setter 才告警；告警沿最短调用过程传到上层可报告函数。
  - [ ] 真实标签复刻、子 Agent 审计、修复复审、提交和推送。
- [ ] 模块 7：生成报告。
  - [ ] 同一结果生成文本、JSON 与标签修改预览，数量与顺序一致。
  - [ ] 输出检测数量、源码标签数量、两类差异、豁免复查、缺少 Reason 和各阶段耗时。
  - [ ] khengine 完整报告验收、子 Agent 审计、修复复审、提交和推送。
- [ ] 最终验收：khengine 每种实际写法都有对应测试，每项标签差异由 AI 复查归因。
- [ ] 最终验收：复核真实日志注入器排除范围与本工具统计范围的一致性。只读检查发现普通与 Vulcan 注入器都排除 `Tss` 命名空间，khengine 当前确有相关源码；当前目录仍会统计其中原本可报告的方法。必须按“日志原本不记录的不提标签建议”核实处理，并保留类级 NLT 的独立审计要求；不把统计范围差异伪装成行为判断结果。
- [ ] 最终验收：`-j1/-j2/-j4/-j8` 结论和顺序一致，统计包含编译的完整耗时。
- [ ] 最终验收：必要调用全部闭合、代码规模与函数注释达标、完整版本已提交并推送。

### 第三模块的真实写法对应

| khengine 实际代码 | 对应行为测试 |
| --- | --- |
| `KHCheckSum.Update` 的成员赋值、遍历和委托 | `WriteField`、`AddAndReturn`、`UseSequence/UseArray`、`InvokeDelegate` |
| `KHPVZGridData.Load/Clear/GetCell` 的二维数组 | `ReadMatrix`、`WriteMatrix`、`CreateMatrix`、`MatrixAddress` |
| `KFLVirtualMachine.Call/Evaluate` 的资源释放 | `UseResource`、`UseResourceDeclaration` |
| `VkeyTracker.Stop` 的锁、遍历和条件调用 | `UseLock`、`UseSequence`、`ConditionalCall` |
| `KHPlayerVKeyComponent.isVKeyDown` 的条件访问与 out 局部变量 | `ReadAsyncReadsOutputWritesAndConditionalOutCalls` |
| `StatusFactory.InitializeStatus` 的类型字典注册 | 字典索引器的接收对象、键、类型值与调用对照测试 |
| `KHRandom.ms_impl` 的隐式静态初始化 | 静态构造函数的对象创建、集合调用和字段写入对照测试 |
| `BuildMonsterPackArg.GetGroupIdOptions` 的延迟遍历 | `YieldWrite` 的源码延迟函数体、托管工厂与真实遍历函数对照 |
| `PveCommonTssMonitor.OnMonsterCreate/OnPlayerCreate` 的捕获值注册 | `RegisterCaptured` 的源码捕获值与托管生成闭包对照 |

本表是已经检查的代表性对应，不代表最终“每种写法”验收全部完成。迭代器的执行时机已记录，但实际创建、遍历与效果传播仍由模块 4/5 实现；没有为未命中的普通异步函数增加额外执行模型。

第四模块后续测试材料已只读确认：`RtTimer` 构造函数保存 `m_callback`，由独立入口 `_OnEnterFrame` 调用；`KFLTriggerEventProxy.Listen/Unlisten` 保存或清空回调，本身不是调用回调的证据。这些写法要求跨函数字段来源；`KFLCommonFunctions.ReflectionInstanceCall*` 直接调用依赖中的 `ReflectUtil.Invoke`，后者先查方法再通过 `MethodInfo.Invoke` 调用；`ReflectUtil.GetMemberInfo` 先查字段、再查属性、再递归基类，并经 `out` 返回结果。这些都必须进入普通来源与调用流程，不为包装函数写特例；当前尚未通过对应模块验收。

当前 khengine 响应文件明确使用 C# 9。测试中默认新版编译器的静态方法组会多产生静态缓存字段，已经单独保留未闭合回归；它不能冒充当前 khengine 已出现的全部写法，也不为编译器生成的某个字段名加特例。实际命中时应进入相同的静态字段来源规则。

### 已有验收证据的有效范围

- 历史可回滚版本 `fd01fc4` 完成前三模块：Debug、Release 均 81/81，15,263 个 khengine 可报告根函数完成行为读取；四种并行数量的数量和行为结果一致。
- 该历史版本已编译工具的 `-j4` 墙钟为 7.869 秒，工具强制重编译另为 1.55 秒；它采用重构前的两套行为读取，没有包含当前的源码内存编译，也没有包含模块 4–7。这些数值不能当作当前版本或完整工具的性能成绩。
- 当前 CLI 仍只连接前三个模块。当前迁移、调用目标开发、真实项目验收的状态以下方记录为准；旧 PASS 不代替新实现验收。

### 统一读取的选择依据

只读实验确认 18 份真实源码均能在内存生成 DLL，源码位置与编译函数能唯一配对。这使源码与 DLL 能共用一套行为读取，删除约 2,900 行重复算法。根编译在新进程中的对照由 11.354 秒降到 5.673 秒，采用的优化是服务器内存回收与关闭分层即时编译；不是重复编译的热态成绩，也没有跨运行缓存。该实验不代表完整性能验收，5–10 秒目标仍待测。

当前材料读取已增加内存编译及独立耗时。源码顺序由真实响应文件决定，新增回归先失败再修复。行为读取已删除源码分支，目录保留源码函数身份、标签、位置，并绑定本次编译的具体函数；磁盘输出不存在或损坏时也不会读取旧文件。编译器生成函数参与调用分析，不进入报告范围。

### 本轮验证与参考

- 统一读取迁移、标准成员编号配对及短指令归一已分别获得限域 PASS；行为读取不再保留第二套源码算法。当前真实输入已完成前三模块读取，完整工具仍须后续模块验收。
- 目录的错版本门面、错版本继承、同签名接口干扰和缺失无关接口回归通过；没有公开测试穿透内部实现的友元声明。
- 最近一次冻结后的 Release 全套 181/181（75.6 秒），编译 0 警告、0 错误；该快照 Core 9,081 行。材料运行文件优先映射 38/38、目录收缩 33/33，并获独立限域 PASS。调用目标仍有明确待修问题。通过数量只覆盖已有用例，不代表模块或真实项目通过。第四模块仍不算完成，没有最终 Setter/Getter 报告。
- 此后由主线程完成并验证：显式接口函数的真实名称配对（目录 35/35）、参数及返回值的真实类型归一和嵌套转交（行为与调用 116/116）、普通调用移除重复继承计算及单次运行内签名缓存（定向与调用 61/61）、DLL 间接引用应用同一已证明的 Unity 版本规则（材料与定向 40/40）。子 Agent 仅负责只读审计，所有新增实现和测试由主线程完成。
- 本轮完整复审又打回接口和重写关系的旧签名比较。主线程先复现漏配，再统一读取方法参数、返回值、泛型继承实参的真实类型身份；显式实现同样读取原始声明引用，不使用门面名称猜测。一次继承遍历同时保留原始实参与真实比较实参，不再重复遍历。隐式/显式接口、普通/多层重写、嵌套泛型与泛型重载共 9 项定向验证通过；目录与调用组 101/101 通过。行为层补足本地定义标记、外部完整来源、插值输入与返回共享对象的断言，独立复审 PASS。
- 本轮完整套件先为 **200/200**（84.726 秒），完整格式检查通过，强制重编译 **0 警告、0 错误**（2.65 秒）。随后主线程完整差异检查删除残留的 decimal 名称特判，让所有命名结构体都保留来源；新增失败回归修复后全套 **201/201**（76.254 秒）。又修复按程序集重新读取源码类型时丢失已回贴位置、标签和身份的问题，两条读取路径均取已发布目录，目录及行为定向 **47/47**，独立复审 PASS。这些工作区测试包含第四模块开发用例，不代表第四模块通过或已交付。
- 工作区最终 Release 全套 **202/202**（85.500 秒）。本检查点仅包含前三模块统一读取及其测试、规则和文档；第四模块主文件、测试及专用测试素材保持未提交开发状态。已导出精确暂存的代码独立验证：Debug **141/141**（68.270 秒）、Release **141/141**（74.780 秒），完整格式检查通过，Release 强制重编译 **0 警告、0 错误**（1.53 秒）；独立限域终审 PASS。当前工作区 Core 9,341 行，本检查点为 7,165 行；完整工具和性能目标仍未完成。
- 最新真实输入在 `-j1/-j2/-j4/-j8` 下的统计数量完全一致：18 份源码程序集、4,522 个源码文件、386 份外部编译引用、30 份分析器、48,694 个源码函数、57,903 个类型；15,267 个 khengine 可报告函数全部完成行为读取。这里只核对真实运行输出的数量；完整公开行为顺序由对应并行回归测试验证，没有虚称比较了尚未生成的最终标签报告。
- 去掉关系匹配中的重复展开后，当前真实前三模块总耗时分别为 **22.151 / 15.772 / 14.001 / 13.193 秒**。`-j4` 为材料 10.426 秒（含源码编译 8.788 秒）、目录 2.309 秒、行为 1.235 秒，工具强制重编译另为 2.65 秒。此前本轮 16.420 秒和关系修复后首跑 20.910 秒均为中间快照。当前性能仍超过 10 秒，且尚未包括第四至第七模块；不宣称达标。
- 精确暂存导出的最终检查点在独立目录重新运行 khengine，`-j4` 为 **18.226 秒**：材料 14.068 秒（含源码编译 11.730 秒）、目录 2.853 秒、行为 1.196 秒，数量与上方四路复测相同。该次测试前已结束其他编译和测试。记录实际波动，不以先前较快结果替代本次验收；性能仍不通过。
- 独立复审打回的两项基础问题均已修复并获限域 PASS：源码依赖使用本轮内存产物；参考转交沿完整身份闭合单跳、多跳和同名不同版本。当前根的 120 个纯门面共同引用 netstandard，已改为本轮线程安全共享一次转交表读取；不跨运行缓存。迁移尚未提交。
- 此前缺少 `TSNinjaSkinBranchScript` 的问题已由外部更新响应文件解决。工具没有修改 KiHan，也没有自行补源码或回退旧 DLL；当前输入数量以上方本轮四种并行数复测为准。
- 网络库错版本已有当前安装内的依据：`Data/il2cpp/external/mono/mono/metadata/assembly.c` 的 `framework_assemblies` 与 `domain.c` 的 `supported_runtimes` 明确给出普通框架库版本重映射。读取实际表项及目录统一消费已实现并获限域 PASS；不按目录相似、文件名或单个库名字放宽版本。按需转交修复后材料 34/34、目录 28/28 通过，真实复跑已越过未使用 `System.CodeDom` 的缺失记录。
- 真实输入的 `Microsoft.Win32.Registry` 与 `Queue<T>` 问题已修复：当前先选真实载体，已撤回把编译参考和运行文件当成平级运行分支的复杂处理。`System.Web.Util.HttpEncoder` 暴露的同一真实文件同时存在本地定义与转交记录，也已按当前 Unity Mono 源码修复：`mono/metadata/class.c:7612–7660` 登记后以同名转交覆盖按名查找记录，`:7946–7987` 按该记录选择目标；本地显式定义编号仍指原定义。目录、普通调用、字段隔离、委托基类转交均有公开回归，独立限域审计 PASS。后续显式接口、嵌套签名、间接框架版本与接口关系问题也已建立失败回归并修复，当前四种并行数都读完前三模块；迁移尚未提交。
- 接口索引复审仍打回：第一次读取时强制闭合所有外部具体类型，既可能漏掉载入过程中新增的类型，也会要求无关依赖闭合。下一步应先用本次接收对象缩小候选，再显式准备必要的继承关系；不能以“工具没找到某个文件”直接排除真实候选，也不能静默使用不完整索引。
- Unity 2021.3 对系统门面的版本统一有[对应官方实现](https://github.com/Unity-Technologies/mono/blob/unity-2021.3-mbe/mono/metadata/assembly.c)。本机编辑器日志确认运行目录是 `unityjit-win32`；独立 `mono.exe` 的默认 `net_4_x-win32` 不能当作编辑器目录依据。门面规则只用于已验证的当前运行目录纯转交文件，普通业务 DLL 仍严格匹配完整身份。
- [.NET NativeAOT 的函数依赖扫描](https://github.com/dotnet/runtime/blob/main/docs/design/coreclr/botr/ilc-architecture.md)也从入口逐步展开实际函数依赖，可参考这个组织方式；它不是 Setter 判断器，不能直接拿其结果当本工具结论。
- [.NET ILLink 的反射读取](https://github.com/dotnet/runtime/blob/main/src/tools/illink/src/linker/Linker.Dataflow/ReflectionMethodBodyScanner.cs)会分别记录接收对象和参数，并交给统一反射操作处理。这支持“先弄清实际对象、参数和目标，再判断影响”的方向；其中未知值和裁剪告警策略不能照搬，本项目仍禁止把未闭合改成最终结论。
- [ECMA-335 第二部分 14.6 节](https://www.ecma-international.org/wp-content/uploads/ECMA-335_6th_edition_june_2012.pdf)明确规定委托构造由运行时提供，保存绑定对象和函数指针；构造不等于调用。该规则必须结合真实委托类型、构造指令和绑定输入使用，不能只看到函数指针就把普通构造函数当成无外部修改。
- [.NET 官方对参考程序集的说明](https://learn.microsoft.com/en-us/dotnet/standard/assembly/reference-assemblies)区分编译契约和执行实现。2026-09-07 本轮据此纠正纯参考门面的处理顺序：已验证的当前运行载体才进入行为读取，不同时分析编译参考所描绘的另一条“运行路径”。
- 已只读核对当前 Unity 自带 Mono 的 `mono/metadata/icall.c:1972` 与 `:2081`：反射字段写入最终根据字段属性写静态存储或目标对象。后续反射处理据真实成员身份产生普通字段写入，不把 `ReflectUtil` 或 KFL 包装函数的名字当作判断规则；这段核对不是反射模块已实现的证明。
