# SetterCheckerNew 设计与进度

## 一、最终目标

SetterCheckerNew 只服务于 `D:/KiHan/Packages/khengine`。

它读取 khengine 当前真正使用的 Unity 编译参数，检查所有相关源码和编译后的 C# 文件，最后回答：

1. 哪些可报告函数的真实行为是 Setter。
2. 哪些可报告函数的真实行为是 Getter。
3. 分析结论与源码现有标签有哪些差异。
4. 哪些人工豁免需要复查，哪些裸 `[NoLogTrack]` Setter 缺少 Reason。

工具生成文本报告、内容一致的 JSON 报告和标签修改预览，不自动修改 KiHan。

## 二、已经确认的判断规则

真实行为只分 Getter 与 Setter。以下行为属于 Setter：

- 修改当前对象、传入对象、对象图、静态数据、集合、数组、事件或 `ref/out` 数据。
- 调用函数后实际修改了调用者已有的对象、参数或静态数据；记录被调函数修改的是接收对象还是第几个参数，再映射回调用者，不能无条件复制被调函数的 Setter 结论。
- 创建 khengine 业务对象并将它返回、保存、注册或传给外部。

只修改函数内部创建且没有离开函数的临时对象，不属于 Setter。

普通根函数从符合初始化约定的状态开始：私有静态只读字段保留已证明的初始化值；不假设另一个独立入口在此前通过反射暗中破坏了这个约定。这是本次验收的输入边界，不是从“没有搜索到字段名”推导出的事实。主 Agent 依据 khengine 当前可见用途自主确定：具体反射写入主要操作业务数据；另有通用入口允许任意成员，所以不能声称这种能力不存在。

当前根函数中实际可达的普通写入、反射写入及未知可写调用仍照常处理，并覆盖初始化事实；不得忽略。数组初始化只用于证明长度等数值，不改变其属于共享静态对象的事实。初始化含未证明调用、重入、外部读取或异常路径时不给出初值证明；未闭合的反射写入也不能据此判成 Getter。

只有函数自身没有外部修改，并且全部合法调用目标的实际影响都闭合、没有造成外部修改，函数才是 Getter。不能闭合时保留准确失败证据，不为该函数给出最终结论；该轮验收失败，但报告仍应列出已证明项目和剩余问题。禁止猜测、保守判定或把部分结果当成全量结论。

`ShouldTrack`、`NoLogTrack` 和 `NLTClass` 是第六模块结合真实行为与源码标签后得出的最终日志决定，不是函数真实行为的别名。`[LogTrack]` 只强制最终决定为 `ShouldTrack`，不能把真实 Getter 改写成 Setter。

## 三、现有标签规则

- 方法级 `[NoLogTrack(Reason)]`、裸 `[NoLogTrack]` 和类级 `[NoLogTrack]` 都先作为可信人工豁免，最终不追踪。
- 豁免函数内部的写入不再使上层需要追踪；若上层还直接修改数据、调用其它未豁免函数，仍独立判断。比如 `A → B → C` 中 B 有可信 NLT，C 的写入在 B 处停止影响 A 的追踪决定，但 C 单独分析仍是 Setter。
- 工具仍独立检查所有豁免函数的真实行为。
- 标签不改变真实参数、字段写回、返回对象身份和能否正常返回。A 返回 NLT 工厂创建的业务对象，是 A 自己的对外返回，仍需追踪；丢弃结果或仅修改内部新对象不增加追踪。A 修改工厂返回的已有对象或将结果保存到已有对象，仍需追踪。
- 类级豁免下的可报告函数标记为 `NLTClass`。
- 带 Reason 的 Setter 和类级豁免 Setter 进入“可信豁免复查”，不报警。
- 只有裸 `[NoLogTrack]` 被独立分析为 Setter 时，告警“缺少 Reason”。
- 是否有 Reason 读取当前标签构造函数真正收到的原因字符串；不能用“括号里有参数”代替。当前 khengine 还存在只有 `NoLogTrackFlag` 数值的写法，它没有提供 Reason。
- 该告警传到所有能调用它的上层可报告函数；每个函数只列一次最短调用过程。
- 没有 NLT 或强制 LogTrack、且标签生效后没有剩余追踪影响的函数进入补标建议；其真实行为可能是 Getter，也可能仅因调用可信豁免而是 Setter，报告必须保留区别。
- 同一函数或其所属类同时出现互相冲突的 NLT 与 `[LogTrack]` 时，正式分析失败并指出冲突位置，不猜测哪一个优先。

现有标签不作为真实行为分析的答案。第六模块复用原调用关系与修改判断，独立求取标签生效后的追踪结果；不另建图，不复用第一遍 Setter 的冻结证据。真实写入证据和追踪证据分别保存。中途报告只列已知事实，受豁免影响但尚未检查的追踪决定明确等待，不能生成补丁；最终报告重新检查，不继承等待状态。

## 四、统计范围

“可报告函数”指进入最终数量统计和标签差异的函数，包括：

- 普通命名函数。
- 运算符和类型转换函数。
- 属性 setter。
- 自定义事件 add/remove。
- 类级 NLT 下原本可报告的上述函数。

构造函数、析构函数、属性 getter、接口或抽象声明、没有函数体的 `extern` 声明、局部函数、匿名函数、编译器生成函数、`getInstance` 和 `BaseProxy_*` 不进入标签统计，但仍参与调用分析。按实际日志注入器共同规则，真实命名空间为 `Tss`、`UnityEngine` 或 `UnityEngine` 子命名空间的函数也不提标签建议；不把 `Tss.Child` 或其他命名空间中的同名类型误排除。类级 NLT 的独立复查要求不因此取消。

当前 khengine 中，程序集声明 `includePlatforms` 仅为 `Editor` 的源码程序集不进入日志标签统计，但源码、内存编译结果和依赖调用仍保留。依据：普通日志注入器默认只处理 `khengine.define`、`khengine.runtime`；Vulcan 读取 Player 程序集列表。当前 `khengine.editor.asmdef` 明确只支持 Editor，已有源码/DLL调用和格式错误回归，限域审计通过。这不是把编译输入改为 Player，也不声称已经实现任意 Unity 平台组合筛选。

## 五、七个模块

生产代码按以下七个模块组织，一个模块一份主文件。主文件可以包含本模块紧密相关的小类型，不再按类拆文件。

### 1. `MaterialLoader.cs`：找齐材料

- 找到 `khengine.runtime.asmdef` 当前唯一的 Unity 编译参数文件。
- Unity 保留多次构建材料时，读取 `Library/Bee/tundra.log.json` 首行明确记录的脚本构建图，验证图和对应响应文件存在；不按时间或目录名猜选。构建记录无效则停止，没有记录且多份候选也停止。
- 读取实际源码、条件编译标记、分析器引用、源码依赖和外部编译文件。
- 读取同一次 Unity 构建图明确列出的全部 C# 源码程序集及其引用，但只有 khengine 进入最终统计。只沿 khengine 的依赖方向会漏掉 Assets 提供的接口实现和回调；只连源码产物也会漏掉共享预编译 DLL 的消费者。因此不在材料阶段猜哪些程序集不相关，后续按真实调用读取必要函数体。
- 源码身份只接受当前构建图的真实产物到编译参数文件的对应；响应文件实际输出必须与其构建节点一致。邻近存在旧参数文件不代表它参与当前编译，不能据此把图外 DLL 当作源码重新编译。
- 保留 Unity 响应文件中的完整编译选项、分析器和源码生成器；生成的源码与普通源码一起进入材料。
- 保留响应文件的源码顺序，把当前源码编译到内存，不读取或覆盖磁盘上的 Unity 输出；编译耗时单独列出并计入总时间。
- 源码依赖按依赖顺序分批编译，互不依赖的程序集并行；上层使用本轮依赖的内存产物，不能用同名旧参考文件的成员表代替当前源码。源码新增或删除成员均进入本轮编译验证。
- 参考文件以及类型转交目标映射到同一 Unity/.NET 环境中的真实承载文件；普通间接依赖先进入候选表，实际命中时按已核实的宿主装载规则查找，不用编译引用的版本替代运行事实。
- 先按当前 Unity 运行目录与已证明的程序集身份规则选择真实载体，参考文件即便只有类型转交记录也不能绕过这一步。参考文件继续供编译器使用，不与已选定的真实载体并列成两个运行分支。若真实载体自身继续转交，则沿它的真实记录查找。
- 只有完全没有同名运行载体的纯参考门面才保留为查找类型终点的别名材料，不贡献函数实现。普通参考文件缺少实现、身份不符或有多个实际载体仍明确失败；不能从编译参考补出真实运行库没有的行为。当前 Unity 编辑器的运行目录由实际编译配置与编辑器日志核实。
- 转交表中能唯一定位的载体继续进入候选范围；尚未使用且没有载体的转交记录保留，实际调用、继承或接口命中该类型时才强制查齐。不能因为参考门面还列有从未用到的类型，就读取其他平台或无关工具目录来补齐。多个合法载体、已登记的循环或身份冲突仍明确失败。
- 找不到、找到多份或身份不一致时直接失败并列出证据。

当前 Unity 2021.3.16f1_0 编辑器已核实使用非严格 Mono 装载：探测按实际文件名，显式运行引用另有元数据名称入口，版本、公钥不单独排除候选。参考根和 `.ref.dll` 不获得预载身份；源码程序集也参与同名竞争。材料、类型转交和函数目录共用同一来源索引，实际需要的多个载体明确失败，尚未使用的竞争不作全局选择。已删除不同代 IL2CPP Mono 源码中的旧框架版本表。当前运行目录没有重复文件名；若将来同运行目录出现该情况，材料仍明确失败，此项尚未改为按需失败。

### 2. `MethodCatalog.cs`：建立函数总表

- 源码内存产物与外部托管文件共用一套类型、参数、泛型和继承事实读取。Roslyn 与 Cecil 用标准文档成员编号配对；只在文档显示副本中去掉源码编号没有的编译修饰符，并补齐嵌套泛型显示结构，不改真实元数据或函数身份。源码只回贴位置、显示名、标签和统计范围，不能猜配对。
- 为每个源码命名函数保留逻辑身份；外部及编译器生成函数以“真实文件 + 元数据标记”天然唯一，在实际调用或候选类型需要时读取并在本次运行内保存，避免启动时创建几十万个无关对象。
- 记录类、函数、参数、文件、行号、标签和是否进入最终统计。
- 预先建立全部类型的继承与接口实现索引，以及按声明类型查找源码函数的表；函数保留真实虚函数标记、参数和实际元数据位置。重写与接口目标只在第四模块按当前类型实参求取，删除重复预存的 RelatedMethodIds 快照。外部候选类型一旦被命中，就从同一真实文件读取该类型的函数和方法实现表。没有生产读取者的额外名称、身份和反向关系表不预建；后续反射候选必须包含实际按需载入的 DLL 函数，不能拿仅有源码的闲置索引代替。

### 3. `BehaviorReader.cs`：读取函数行为

- 源码先由 C# 编译器转换成当前内存中的函数内容；源码位置、标签和可报告函数身份仍由源码目录保留。
- 当前源码的内存编译结果和真实托管动态链接库共用一套函数指令读取，不保留第二套源码行为分支或回退路径。
- 统一记录字段写入、属性写入、数组或集合写入、事件修改、`ref/out` 修改、调用、对象创建、返回、保存、注册、传出和对象来源。
- 每次普通变量读取都保留独立快照、所在基本块和执行顺序；赋值、调用、写入、返回使用同一套位置。变量的稳定存储身份只用于取地址、引用传参和闭包捕获。保留编译器给出的分支、异常处理和最终释放关系，不能只记源码行号或合并所有历史赋值。
- 这一模块只描述事实，不直接给出 Getter、Setter 或标签结论。
- 正式运行先读取 khengine 可报告函数，依赖函数体在模块 4 找到实际目标后补读；依赖类型和函数目录仍预先建立。
- 迭代器按编译器实际生成的创建函数与遍历函数分别读取；创建时不执行遍历函数，不手写第二套状态机或捕获规则。编译器生成函数参与调用分析，但不进入标签统计。

### 4. `CallTargetResolver.cs`：找齐调用目标

- 依据第三模块保留的执行关系，统一计算每个使用位置真正能到达的值来源；委托、反射与第五模块共用这份结果，不各自重复实现一套变量分析。
- 普通调用连接到明确函数。
- 接口调用连接到全部合法实现。
- 可重写函数连接到全部合法重写。
- 委托连接到全部可能绑定的函数。
- 事件回调与委托使用同一套绑定目标规则。
- 反射按类型、名称和参数解析字段、属性、函数与构造函数。
- `GetMethod("Foo")` 只匹配合法的 `Foo`；名称来自参数时追踪全部调用点传值。
- 反射名称允许由外部任意传入时，检查当前代码闭包内所有类型和签名能够匹配的成员。
- `FieldInfo.SetValue` 和 `PropertyInfo.SetValue` 产生普通写入关系，不硬编码最终结论。
- 每找到尚未读取的目标，就交给模块 3 补读，再继续解析它的调用；直到必要关系没有新目标。模块 4 与 5 联合验收，运行时共用已有调用和值来源。
- 普通调用迭代器函数只创建可遍历对象，只有实际遍历才连接其延迟函数体；不得在创建时直接传播函数体中的写入。

本模块按以下顺序实现和验证，不同时铺开多套算法：

1. 先准确接上普通调用和构造函数，复用函数总表中的身份与类型信息；每轮集中补读新命中的函数，已经读过的不重读。
2. 接上接口与可重写函数；候选表按真实继承、实现关系建立，不按函数名相似程度匹配。
3. 利用第三模块的执行顺序，计算一次变量在使用位置可能来自哪里。地址仍连接原变量，普通读取保留当时的值；这份结果供委托、反射与下一模块共同使用。
4. 接上委托、事件和反射；实际目标与实际接收对象、参数一起保存，不能只列出一个函数名而丢掉调用方式。
5. 没有新目标后固定结果顺序。保留全部参与分析的函数、已读行为、逐调用点的目标、共用值来源及耗时，不提前输出 Setter/Getter。

新写法加入本模块已有的目标读取或值来源处理，不新建插件、规则注册框架或按业务函数定制分支。每一步先做源码和真实 DLL 的对照测试，再用 khengine 检查实际调用。

2026-09-09 修正证明顺序：第四、第五模块共用已有调用关系和值来源。确定写入还必须有到达该位置的执行证明，不能把可能不返回的调用之后的写入当作既定事实，也不能从另一条分支借用对象来源。满足这些条件后，写入后面的独立未知调用不会推翻已经发生的修改。被调函数修改自身仍须按实际参数映射，不能直接复制给调用者。Setter 证明后继续保留标签审计和必要的待处理调用；Getter 必须闭合全部相关目标。相同且不影响判断的泛型实参应共享处理，但不同参数环境不能合并。当前只有专项测试和限域审计通过，不代表整个模块完成。

同日后续复现出四项整数条件误判：同值相减、按位与零、同一函数内互相矛盾的比较，以及跨函数传参后的矛盾比较。补充通用条件检查仍留在第四模块，不另建调用或变量分析图。使用统一版本的 Microsoft.Z3 4.12.2 表达 32/64 位整数运算，复用既有参数身份、赋值来源与前驱边；临时验证已确认本机装载及单线程配置正常。先验证排除不可能路径，再补齐写入对象与合法路径的联合证明；仅公式有解不能替代真实执行证明，未支持的循环、返回关联、异常或原生来源不能包装成任意整数。所有公式仅在一次冻结材料查询内有效，不建立跨轮缓存。此项进行中，不能继续声称“条件来源已闭合”等于“写入分支确实能执行”。

2026-09-07 根据用户要求自主决定：按被调用函数实际修改的对象向上传递影响。修改静态数据或调用者已有对象时传播；只修改调用者内部新建且没有传出的对象时不传播。参考 `Runtime/KFL/DSL/KFLCsLuaASTEncodeUtil.cs:135`：内部新建 `StringBuilder` 并调用 `Append`，最后返回字符串，拼接对象本身没有传出。仅这条拼接路径不能因为 `Append` 会修改自身，就判定上层修改了外部状态；其它调用仍独立检查。

### 5. `EffectAnalyzer.cs`：得出真实结论

- 先标出直接修改外部状态的函数。
- 再沿调用关系传播实际修改的对象和参数，逐调用点判断是否影响调用者外部状态。
- 跟踪新对象是否只在函数内部使用，还是被返回、保存、注册或传出。
- 循环调用使用同一轮稳定计算，不重复扫描全部函数。
- 全部关系闭合后，每个函数的真实行为只有 Getter 或 Setter 两种结论。

### 6. `AnnotationEvaluator.cs`：处理标签和差异

- 应用可信 NLT 与 `[LogTrack]` 规则；源码和 DLL 标签从同一份真实元数据读取。
- 仅对含可信豁免调用、真实为 Setter 且自身未豁免的入口复用第五模块，检查是否还存在需要追踪的修改；保持原业务程序集范围。真实行为不重新定义，调用和对象关系不重建。
- 生成可信豁免复查、裸 NLT 缺少 Reason 告警、上游调用过程和可补标清单。
- 只在这一模块决定标签规则，不改变前五个模块的真实行为分析；追踪未证明与真正的标签冲突分别报告。

### 7. `ReportWriter.cs`：生成交付结果

- 输出总函数数、检测结果数量和源码标签数量。
- 按类汇总两类主要差异。
- 输出可信豁免复查、缺少 Reason、失败证据和每阶段耗时。
- 已绑定原生调用也必须披露，不能因为入口已有独立 Setter 证据就从报告中消失。按根函数和目标分组，保留真实库/入口、运行时来源、调用绑定数，以及该根是否已有独立的真实结论和日志决定；只有后者可延期，未证明根不能标成无影响。已按真实语义处理的委托构造不列为未解决边界。
- 从同一结果生成文本、JSON 与补丁预览，顺序固定。

## 六、代码目录

仓库只保留三个项目：

```text
Src/SetterChecker.Core/
  SetterChecker.cs              总入口、共享数据和七个模块的顺序
  MaterialLoader.cs
  MethodCatalog.cs
  BehaviorReader.cs
  CallTargetResolver.cs
  EffectAnalyzer.cs
  AnnotationEvaluator.cs
  ReportWriter.cs
  SetterChecker.Core.csproj

Src/SetterChecker.Cli/
  Program.cs
  SetterChecker.Cli.csproj

Tests/SetterChecker.Core.Tests/
  MaterialLoaderTests.cs
  MethodCatalogTests.cs
  BehaviorReaderTests.cs
  CallTargetResolverTests.cs
  EffectAnalyzerTests.cs
  AnnotationEvaluatorTests.cs
  ReportWriterTests.cs
  TestProject.cs
  SetterChecker.Core.Tests.csproj
```

不建立空目录、插件系统、依赖注入容器、适配层、一类一文件结构或未来需求占位。生产入口不支持任意普通 `.csproj` 或通用 NuGet 工程；自动化测试仍可建立最小 C# 夹具验证源码与动态链接库的同一写法。`bin/Debug`、`bin/Release`、`obj`、`artifacts` 和 `TestResults` 都是临时生成内容，不属于源码，不提交。

手写生产代码以 3000–5000 行为优先目标。用户于 2026-09-09 将 Core 全部手写 C# 上限调整为 13,000 个物理行（包含注释、空行，排除 bin/obj）。不能为了压行数牺牲功能和可读性，也不能靠移到 Cli、拆文件或压缩排版绕过上限；必须报告实际行数及超出优先目标部分的用途。

模块 3 首轮实现使手写生产代码增长到约 8,300 行，复杂度审计判定已经偏离目标。模块 2 因此重新打开：改用项目统一版本的 Mono.Cecil 读取托管元数据和指令，删除自行实现的元数据框架、非 khengine 边界样本和重复身份结构。压缩不能删除真实项目需要的分析能力；预计超过 5,000 行时，必须先列出不能省略的部分和当前精确行数。

每个函数的 XML 文档块之前写一行简短中文 `//` 注释，直接说明这个函数的用途；没有 XML 文档时注释紧贴函数声明。公开函数仍按统一编码规范保留必要的 XML 文档，但不写重复的大段说明。

## 七、外部代码与动态调用

- 当前 Unity 编译参数列出的源码和托管动态链接库都必须读取真实内容。
- 只有声明的参考文件必须映射到当前运行环境真实文件后再分析。
- 包装函数通过自己的源码或函数指令自然进入调用关系，不为 KFL、ReflectUtil 或具体业务函数写特例。
- 接口、重写、委托和反射统一遵守“全部合法目标都闭合”的规则。
- `Activator.CreateInstance` 解析传入类型及构造函数，并继续判断新对象是否离开函数。
- 第一轮不使用 LogicData 限制反射目标，不枚举未启用的条件编译组合。
- 如果某个可报告函数的结论依赖原生函数体，正式分析失败并指出调用过程；不影响任何可报告结论的原生边界才进入延期清单。
- 运行时生成新代码或加载当前材料范围外的程序集时直接失败，不提供兜底结果。

## 八、禁止的实现方式

- 禁止按某个业务函数名、类名或报告差异硬编码 Getter、ShouldTrack 或忽略结论。
- 禁止 `Unresolved => ShouldTrack`、`Unresolved => NoLogTrack` 或任何保守兜底。
- 禁止捕获所有异常后继续分析、缺文件时扫描全盘、按相似文件名猜实现。
- 禁止为未出现的输入格式、旧版本、插件能力或跨运行缓存预建抽象。
- 禁止为了“职责纯洁”把一段连续流程拆成大量只转发一次的小类。
- 必需的路径、文件、程序集身份和目标闭合校验不属于防御性编程，必须保留；禁止的是猜测、兜底和额外替代路径。

## 九、多线程和性能

命令行接受 `-j4`、`-j 4` 和 `--jobs 4`。数值是整个工具共用的最大并行工作数，不写死为 4。

默认工作数为 4，任意正整数有效，0 或负数直接拒绝。源码文件、外部文件、函数行为和动态目标等互不依赖的项目使用同一个并行上限。先找齐材料、建立目录，再由模块 3 与模块 4 补齐实际调用链，最后判断行为、处理标签和生成报告。

编译器内部并行统一关闭：当前编译器的内部队列没有按本工具的 `-j` 限额执行，不能仅凭“一份程序集”就放开。外层按 `-j` 同时编译独立程序集，每份完成后立即发布给后继，不等待整层全部完成。异常退出时收拢已经启动的工作，并保留原始失败。每一步结束后固定排序，因此 `-j1`、`-j2`、`-j4`、`-j8` 必须得到逐字一致的结果。

允许一次运行内使用函数、类型、程序集和调用关系字典，不使用持久化缓存。完整分析目标是 5 秒以内，连同工具编译最低要求是 10 秒以内；每个模块单独计时。

## 十、测试和自动审计

测试只通过模块公开结果或工具总入口验证行为，不检查内部拆了多少函数、调用了几次私有方法。每增加一种能力执行一次“先失败、再实现、再整理”的小循环。

同一个最小函数必须分别从源码和真实托管动态链接库读取，两种来源得到的写入、调用、创建、对象来源和最终真实行为必须一致。

全部代码组织、实现、测试编写与执行由主 Agent 负责；子 Agent 只做只读审计，审计打回的问题由主 Agent 修复，不把实现工作再分派出去。

每个模块必须经过：

1. 用明确预期的小例子覆盖正常情况和当前项目实际边界。
2. 编译、格式检查和全部自动化测试通过。
3. 在 KiHan 中只读查找本模块涉及的真实写法，并建立一一对应的最小测试。
4. 把代码、测试和本文件交给子 Agent 审计。
5. 审计不通过就自动修复并重新送审，直到明确通过；只有审计通过后才提交和推送。在已确认范围内自主判断并记录理由；只有需要额外授权或实质扩大范围时才询问用户。
6. 按最新要求先接通七个模块形成可运行版本，再依据真实报告逐项完善；模块 4 与 5 共用已有调用和值来源。等待审计期间继续独立检查和测试，不空等。
7. 通过后检查完整差异，提交并推送可回滚版本。

最终再用 `D:/KiHan/Packages/khengine/Runtime/khengine.runtime.asmdef` 完整运行：

- 每种真实写法都有对应测试。
- 除确认不影响任何可报告函数结论的原生边界外没有无法闭合项。
- 工具结论和源码标签的每项差异都由 AI 复查归因。
- `-j1`、`-j2`、`-j4` 结果一致，并记录性能。

## 十一、当前评估和执行清单

2026-09-10：七个模块都已接通，但 **尚未完成 khengine 验收**。子 Agent 只读审计，主 Agent 负责实现、复现问题和运行测试；审计提出的删改建议仍由主 Agent 核实实际使用位置，不能直接照搬。

复杂度集中在第四模块：同一个函数在不同对象、参数和执行位置下，结果可能不同。需要保留这些区别，但不需要重复保存身份字符串、重复扫描程序集、为每个中间函数建立第二套修改结果。此次精简针对这些重复工作，没有减少身份校验、把未确定结果转成结论或把生产代码移到命令行项目。

| 模块 | Core 行数 | 当前状态 |
| --- | ---: | --- |
| 找齐材料 | 1,335 | 依赖完成即启动后继；类型导出记录共用读取，删除单次目录转发，保留真实身份及后值覆盖规则 |
| 函数总表 | 2,370 | 源码与 DLL 共用读取；删除单次类型查找转发，共用物理身份 |
| 读取行为 | 1,528 | 指令类型归入实际值和写入，删除单次栈恢复/加载包装；完整异常仍未完成 |
| 找齐调用目标 | 6,923 | 减少重复泛型候选、返回槽扫描和静态字段检查；完整边界及大规模重复绑定仍未完成 |
| 判断真实行为 | 399 | 静态写入保留已证明执行路径，不让无关未知分支推翻正证；标签与真实事实仍分开 |
| 处理标签 | 128 | 分别保留真实行为和标签生效后的追踪决定；等待项不冒充结论 |
| 生成报告 | 160 | 文本/JSON 增加已绑定原生边界及逐根延期状态；全量业务归因尚未完成 |
| 总入口与共享材料 | 157 | 全流程运行与同步进度报告；整体失败不冒充完成 |

Core 当前 **13,000 个物理行**，包含注释和空行，不含 bin/obj；上限为 **13,000 行**。前一开发检查点为 `12b6837`，完整 Release **995/995**。本批最新完整 Release **1003/1003**（361.901 秒），此前 Debug **999/999**（420.842 秒），均零跳过。最新 Release 还包含四项 KH 生成属性对照、实际标签构造，以及返回槽和静态字段检查的两处省重；构建零警告零错误。本批仍仅为开发检查点；真实全量未完成，不能以单元测试或局部省重宣称性能达标。

模块设计复评：第一、二模块的材料身份和函数配对有必要，删除的只是单次转发；第三模块统一指令读取仍值得保留。主要冗余和可靠性压力在第四模块，占 Core 约 53%，目标绑定、存储追查和执行条件共享可变状态，不能称为已经简洁可靠。第五、六模块短小且职责清楚，但“真实写入”和“标签后追踪”两遍计算是必要区别，不能合并掉。第七模块已有机器报告，仍不能代替最终逐函数的业务归因报告。

并行验收还没完成：材料准备、程序集编译、目录和函数体读取已使用共用的 `-j` 上限；第四模块实际动态目标/条件展开和第五模块传播目前仍主要串行。不能把“接受 -j4”宣传成热点已实现四路加速。后续应减少同一入口重复处理，再检查互不共享状态的工作如何并行，不能直接对共享字典开启并行循环。

### 可检查的待办

- [x] 七个模块及总入口全部接通，区分中途报告、最终报告和失败报告。
- [x] 源码与真实托管 DLL 共用行为读取和存储追查，不维护业务函数效果表。
- [x] 接口、虚函数、委托及已支持的反射调用进入同一调用绑定流程。
- [x] 人工豁免与真实行为分离；裸 NoLogTrack 仅在独立分析为 Setter 时告警缺少 Reason。
- [x] 整数保留真实位宽、回绕和入边；对象选择、空值和类型条件在同一次执行中核对。
- [x] 修复大函数追查的进程栈溢出；3,000 次顺序调用对照通过，不按函数长度跳过。
- [x] 查询环境仅在本次批次内复用；不跨入口共享公式、不做持久或增量缓存。
- [x] 静态反射字段读取及同类型多来源的字段名选择已红测修复，并通过限定只读审计。
- [x] 条件回调替换、强写之后抛出、动态实现的对象返回、finally 阻止返回均有源码/DLL 对照。
- [x] 本轮嵌套容器、间接引用写入、返回槽到实际出口的新增反例已修复，通过完整回归和限定审计。
- [x] 仅重算本次运行中取得新事实的入口；共享函数体、类型、转交别名、泛型声明和恢复阶段的唤醒经限定审计。
- [x] 复现并修复 TeaCrypt.Decrypt8Bytes 的数组写入与局部槽互相追查；保留值类型字段、外部引用和混合地址的原查询。
- [x] 引用读取保留真实地址选择；标量截窄、枚举底层类型、泛型实际类型与对象/结构体返回各有源码或真实 DLL 对照。
- [x] switch 的 case、默认分支及共享目标进入原条件判断；固定编号能排除未执行的调用，KH ReadField 方法体复刻通过。
- [x] 修复未决加载标记被再次当指令执行的无限增长；InitPvpConf 首轮报告复现及真实扫描均越过原卡点。
- [x] 根引用参数所指对象保留独立入口身份；新旧对象分支、只读和泛型引用、可能别名均有正反对照和限定审计。
- [x] 本批复现并修复枚举输入、窄整数存储和返回、数组下标与反射实参数组长度；真实 DLL 执行结果与分析结论对照通过。
- [x] 委托构造和调用分支必须相符；三层容器的祖先选择继续传到叶子，集中正反对照通过。
- [x] 反射选中哪个字段必须保留到实际写入；未选中的字段仍取旧值，静态与实例字段不能混成同一修改。
- [x] 本批完整回归、限定审计、格式编码及完整差异检查通过；按开发检查点发布，不标为验收版本。
- [x] 内部调用键编号精简：完整回归及限定审计通过；不以字符串缩短代替全量性能验证。
- [x] 真实扫描暴露的多返回出口旧值续查崩溃已在真实 DLL 双 ret 测试复现并修复。
- [x] 删除重复的新函数唤醒及已确定入口的上游查找；原调用图完整保留，恢复审计重新检查全部入口。
- [x] 调用绑定同时记录递归回边，避免每次整数查询遍历全部调用；整根重置撤销旧记录，查询取得独立快照。
- [x] 首次调用不可能到达任何递归位置时重新展开；源码/DLL、j1/j4、变化参数、原生边界和独立待重查入口对照通过。
- [x] 纠正标签传播口径：保留真实写入供复查，并独立判断可信豁免生效后的上层追踪决定，不仅覆盖自身显示；集中测试及限定审计通过。
- [x] 本批结构体修复检查点：完整回归、专项、限定审计及格式编码检查通过；不等同于完整结构体能力。
- [x] 后续真实扫描崩溃、私人槽查询精简及指令类型归并：最新完整回归、限定复审、格式及编码检查通过，作为开发检查点；不等同于 KHEngine 验收。
- [x] 连续静态开关读取的重复追查崩溃已复现修复；缺失函数体按原工作队列补读，保留参数写回和实际分支。最新完整 Release 回归通过，不等同于全量性能达标。
- [x] 补齐静态写入见证的初始化检查；已绑定原生边界按根展示，不因独立 Setter 证据而漏出报告。源码/DLL 初始化和逐根、j1/j4 报告对照通过。
- [x] 本批省去相同泛型实参的重复查找、已清理实例的重复撤销、返回槽全表扫描及同点静态字段重复检查；新旧对象分支、初始化、恢复分析回归与限定审计通过。
- [x] 复刻 SkillModel.actionID 生成属性：源码/DLL 调用者保留真实 Setter，可信外层豁免不覆盖调用者独立写入；源码分支另验证内层 LogTrack、缺少 Reason 与告警调用路径。
- [ ] 完整循环、异常、跨入口保存的回调、完整反射及实际原生边界闭合。
- [ ] 用最新构建完成 khengine 全量扫描，输出最终报告。
- [ ] 按真实写法建立对应测试，逐项核实报告差异属于分析器问题还是源码标签问题。
- [x] 对照旧版 Lean 八项审计完成代码与测试核查；已处理项和仍缺能力分别登记，不把未误报等同于已经闭合。
- [ ] 完成全部模块后逐项评估设计，合并真实差异复查，在桌面生成最终报告：文件路径、具体原因、实现细节、判定过程，以及确属问题或建议带原因豁免的合理写入（如缓存更新）。
- [ ] 验证 j1/j2/j4/j8 的结论和顺序一致，记录包括编译的实际时间。
- [ ] 全量正确性、差异复查及性能均达到目标后，才标记验收完成。

### 当前验证事实

- `12b6837` 真实扫描于 2026-09-10 15:17 启动，尚未结束。输入已由游戏工作副本更新为 **292,922** 个源码函数、**15,058** 个可报告函数，源码 NLT **3,275**；不能与此前不同输入的扫描直接比较提速。15:40 左右落盘中途快照：检测 NLT **1,709**、ShouldTrack **1,865**、未证明 **11,484**、待处理调用 **413,024**；材料含编译 **72.307 秒**，后续阶段当时已 **1,362.746 秒**。它不包含本批省重，也不是最终报告。
- 当前待绑定队列仍会全表重试。条件复查的工作集不能直接拿来筛选绑定：某个入口剪枝后的全局继续，以及新增普通目标后的动态调用延期，都可能消费了另一入口的条件复查资格，却尚未尝试它的调用。下一步先补这两种公开反例，再改工作资格；不建立失败缓存，不因无新增事实跳过首次分析。
- `SkillModel_CheckSum.cs` 的生成属性真实修改 public 的 `actionID_checksum_replace`，因此 Setter 判定正确；外层 NLT 与内层 LogTrack 表达了避免重复记录的设计意图。生成来源为依赖包 `kihan.fspdebuger/Editor/CheckSum/templates/property_template.txt`，补 Reason 只是说明已有豁免理由，不改变当前追踪。模板在编辑器域内缓存；Vulcan 全量注入还会排除 `_checksum_setter`，所以测试不能保证所有日志模式都实际记录内层。这里只记录来源与复查建议，不修改依赖或运行生成器。
- `e7a9915` 扫描在 `KFBWriter.WriteField(int, bool, bool)` 的静态 `SkipDefault` 读取上反复追查前面调用。128 次调用复刻先出现进程栈溢出/异常退出，后按没有写入的真实静态字段及参数来源省去重复读取，并在所需直接函数体尚未补读时停止该次求值，交回原调用工作队列。没有次数上限、超时兜底或跨轮缓存。最新完整 Release 中该对照约 **7.128 秒**；30 秒断言只防止测试失控，不是全量验收标准。
- 路径已有实际正常返回证据时，可以由绕过未知调用的另一条分支证明 Setter；三元表达式交换左右顺序也必须一致。未知值没有变成任意输入，原调用仍保留。静态写入正证曾绕过旧的类型初始化检查，审计反例已红测：前序静态调用、目标内部写入、直接字段写入、读后丢弃及反射读取统一补查初始化；必经未知初始化后的写入仍不提供证据。
- 未知存储影响仍是待分析来源，但现在保留产生它的具体调用名，不把报告降成只有字段名或内部值编号。原生边界按入口分别记录，即使一个入口已经独立证明为 Setter；同一目标在另一个入口仍阻碍判断时不能共享延期状态。数量指分析期调用绑定数，不是运行次数。
- `e7a9915` 旧扫描 15:14 检查时仍未结束，最近落盘中途快照为 14:59：总数 **15,051**，检测 NLT **1,717**、ShouldTrack **2,246**、未证明 **11,088**、待处理调用 **747,684**；材料含编译 **69.348 秒**，后续阶段已 **3,309.103 秒**。这些不是最终结果，也不包含本批修复，不用于宣称本批成效。
- `87d8c0f` 的真实 khengine 扫描在首轮出现数组越界：内部曾用负编号表示 ref 所指存储，后续却按普通值编号索引函数内容。已用源码及真实 DLL 的根 ref 结构体空值分支复现，改为正编号加“是否所指内容”的独立事实；存储键、字段路径、赋值、别名比较与快捷查询全部保留该区别。未用捕获程序异常掩盖问题。
- 根 ref/in 内容按真实参数类型表示，指针槽与所指初始值分开；out 仍不能借未初始化入口值。新增根 ref 结构体、struct this、引用改绑及旧地址捕获、标量写前快照、空值/非空值条件对照。13:13 启动的真实扫描包含这批引用修复，已经越过原崩溃点；该进程不含随后仍在测试的性能及前序指令检查改动。
- 仅当当前函数的普通变量/按值参数槽从未取地址时，省去其它函数能否替换这个私人槽的重复查询；真实赋值、指向对象内容、取地址后的槽、ref/out 内容和静态初始化仍保留原追查。地址事实按整个原函数登记，不按暂定可达性删减。源码/DLL、j1/j4 的新旧对象、参数取地址、结构体、对象内容及初始化失败专项通过，尚不声称全量性能改善。
- 精简暴露了原执行证明的缺口：前序 ldind/ldobj 与间接写入没有进入正常访问检查。现在同一位置索引记录这些访问；未表示的原生指针和未决值不能冒充托管地址，值类型字段地址还要检查其真实根地址。写入正证可以采用完整证明的其它路径，但被排除路径必须保留未完成标记和原失败原因，不用于证明 Getter 或删除原执行边。`while` 零轮后写入有真实 Setter 见证；必须执行一次指针访问的 `do/while` 后写入仍未证明；访问前写入和 `if` 中完全绕过访问的分支分别验证。此项不代表全部原生指针、字段访问或内存操作已经闭合。
- 删除无生产读取者的独立类型使用清单，将原类型操作数附到实际值/写入的 `OperandType`。保留真实程序集路径及泛型身份，不改表示结果类型的 `Type`，也不把 `sizeof(T)` 的结果误设为 T。原十项类型对照、实际 sizeof(T)/sizeof(U) 和 cpobj DLL 对照通过；根值、汇合值和下一条无类型操作数的指令不继承上一条类型。
- 本批先复现结构体中的引用字段经 ref 写回后被遗漏，再把整个结构体的赋值、嵌套成员覆盖和数组元素替换纳入原存储追查。数组替换为旧对象时曾漏报，替换为新对象时曾误报；两个方向都保留源码/DLL 对照。复制时读取当时字段内容，不把原件后续变化传播给副本；默认结构体不是空引用。
- 求值栈上的结构体副本、嵌套副本及 ldobj 读取各有正反测试，其中 6 项实际执行修改后的 DLL，再与分析结果核对。多层按值传参继续原调用入口查询，没有第二套对象图。单层根结构体取地址后访问引用字段保留正确归属和空值条件；仅在字段未被覆盖、真实存储是唯一根值参数槽时消除取地址包装。
- 根结构体的嵌套地址链仍未完整表示：普通源码按值读取可以是 Setter，而真实 DLL 的 ldarga/ldflda 链目前明确失败，不生成 Getter。结构体互相复制的循环也明确未闭合；精确的活动读取与成员路径检测防止无限递归，不设迭代次数上限，不假装已经求出循环结论。
- 本轮限定设计复查认为主流程合理，复杂度主要集中在第四模块（约 53%）。材料与目录中的身份、类型转交和当前编译校验不能无依据删除。标签第二遍复用原关系是区分真实行为与可信豁免的必要步骤；报告模块仍缺最终逐函数的业务实现复查，名称/位置清单不能代替用户要求的完整桌面报告。
- `9b59080` 旧扫描于 2026-09-10 12:48 停止，专门保留仓库外中途快照后准备切换最新构建。最后落盘结果：总数 15,051，检测 NLT 1,726、ShouldTrack 2,557、未证明 10,768、待处理调用 747,490；材料含编译 66.660 秒，其中编译 58.888 秒，后续阶段当时已 4,686.414 秒。落盘快照是 12:22 的中途结果，不是停止瞬间的完整结论，不适用于宣称本批标签或结构体修复成效。
- 本批统一从真实枚举字段读取底层宽度，不限制为有名字的枚举值。参数、局部槽、字段、数组、间接读取和返回分别在实际加载处截短，数组下标和反射实参数组长度复用同一确定整数读取；不再穿过多次加载直接采用最早常量。
- 窄存储验证覆盖直接读取及随后转存到宽字段，共 22 个实际 DLL 执行对照；另复刻 BtReturnInvert、BtReturnSuccess 和 AI 对战 AddScore 的方法体。外围基类只保留原本的空更新方法及字段，未借这些测试宣称全部基类依赖已闭合。
- 委托目标保存构造对象和绑定函数两项选择，仍交给现有子入口条件；`flag ? Set : Get` 随后仅在 `!flag` 时调用必须是 Getter。三层容器只填充未被返回的叶子也必须是 Getter，不能拼接相反分支。
- 单一直接强写可共用既有值读取，但仍保留传入的选择条件和每个加载边界；未知别名、子调用切点和非直接存储读取继续原追查。分支排除与修改证明使用同一对象读取代码，没有新增分析图。
- 反射字段写入已经生成普通写入时，不再要求空目标提供子函数返回点；条件成员仍携带原选择。针对同一字段的倒查保留未选中时的旧值，原写入证明也核对该条件。审计反例先复现 Setter 被漏报为 Getter，再修复；这不是按业务函数名补规则。
- 已闭合候选全为同一物理字段时，不再保存多余的成员身份选择；无条件覆盖不追查旧值。跨函数原生调用之后再覆盖回调的反例已红测修复：回调可闭合为新目标，整轮仍保留原生调用的未证明状态。
- 内部调用实例键与递归输入键使用仅在本次运行内的一一函数编号，仍保留值、实例、参数位置及顺序；类型参数的共享身份继续使用完整函数名。编号表随原值来源保存，不在恢复入口时清空，不增加新一轮分析；此改动减少重复字符串，不减少需要区分的调用环境。
- 真实扫描曾把“继续读取调用者旧值”的内部标记当成实际函数读取，导致程序异常。真实 DLL 的两个返回出口测试先复现问题，再让该标记继续原续查流程；有写入与无写入分支分别验证，未新增异常捕获或默认值。修复后真实扫描已越过原崩溃批次，尚未完成全量。
- 3,000 次顺序调用曾因无附加条件仍组成空合取而严重退化；直接规范成真后，完整回归中两个用例分别约 0.55、0.76 秒。未缩小测试规模，不用该耗时替代全量性能。
- `fb21f9d` 真实扫描运行近 8 小时仍未结束，已停止旧进程并保留临时报告用于对比。停止前快照：检测 NLT **1,798**、ShouldTrack **2,574**、未证明 **10,679**、待处理调用 **3,103,390**；材料与编译 **58.4 秒**，后续处理 **28,260 秒**。这些是中途结果，性能明显不合格；待处理总数还包含已证明 Setter 暂存给后续审计的调用。

- 本批完整回归 Debug **947/947**（353.865 秒），最新 Release **947/947**（302.532 秒），无跳过。中途一次全量曾因临时 package.json 文件被占用而失败，未确认占用进程；未加重试或吞错，后续两次完整回归均正常完成。前一检查点旧审计精确对照 **8/8**（2.901 秒），此前标签与进度集中回归 **20/20**（6.148 秒）。主线程实现和运行，子 Agent 仅做限定只读审计。
- 标签传播以六项公开入口反例先复现，再修复；源码与真实 DLL 的三种豁免、保留真实字段写回、调用者自己的修改/返回/保存、显式 LogTrack、未知原生调用、跨业务程序集范围以及中途/最终报告均有对应测试。暂不以这些用例代表全量标签差异已经正确。
- 四项精简删除基础类型映射表、无人读取的块/边附加标记、操作数单次转发以及程序集身份转换包装；真实条件、finally、参数 this 偏移及严格身份比较原样保留。基础类型采用 [Cecil 0.11.6 的已有定义](https://github.com/jbevain/cecil/blob/0.11.6/Mono.Cecil/TypeReference.cs#L270)，不是按类型名称认定函数行为。
- 实际进程快照确认：KFBReader 的整数读取反复在创建查询时扫描所有调用。当时存在 251,569 个调用实例、43,621 个活跃调用者；本批改为绑定时登记递归回边。未据此宣称全量性能已经达标。
- 递归排除只在已读、无异常区域且隐式初始化完整的托管闭包内尝试，必须在同一份原图上证明全部再入位置首次均不可达；不得借递归返回值、返回观察链或返回后的存储证明自身。提前返回会归还本轮其他待检查入口，普通证明前清除临时整数结果。真正发生的递归仍须单独闭合。
- InitPvpConf 的函数体保留原判断和赋值，外围 XML 容器替身只用于复现首轮报告，真实 int/uint.TryParse 调用仍在。未决 CallResult 不再被当成 ldind.ref/ldobj 再执行；两种实际 DLL 指令对照都必须保留未证明和待处理项，不借首轮测试宣称库已闭合。
- 外部 ref 所指对象的初值只在倒查到根入口后产生，和地址本身分开；实际赋值与子调用写回仍优先。混合地址逐位置倒查再合并，某个局部变量的赋值不能覆盖其它位置。out 初值、聚合引用初值和未证明的两对象别名不在此次通过范围内。
- TeaCrypt.Decrypt8Bytes 方法体逐句复刻，只有后续 Decipher 依赖替换为返回输入。旧实现长时间不返回，修复后源码/DLL 集中测试约 2 秒通过；该耗时不是完整 khengine 扫描时间。不按函数名或类级 NLT 跳过。
- 存储追查只提前排除已证明不相干的直接托管数组元素：它不能覆盖局部槽、静态字段或引用类型对象字段；值类型字段、外部 ref 所指内容、混合地址和批量写入仍完整查询。固定函数图的正负结果及无循环子查询仅在当前运行中复用。
- 新增反例先复现混合引用漏掉数组支路、先写 ref 再写同一数组元素仍取旧值、窄整数加载未截断、引用及结构体返回丢失新对象，再逐项修复。标量和枚举不提前折叠为写入前的整数；枚举底层类型来自实际字段元数据。部分宽度覆盖和原生指针模型不在这些通过结论内。
- KH_DoGSArg_Serializer.ReadField 的方法体逐句保留，Reader 替换为确定返回值，基类装饰器替身实际调用传入委托。它只验证真实序列化方法写法，不代表实际 Reader 和装饰器内部循环、异常已经闭合。额外 DLL 测试明确制造 case/default 同地址，并验证负数、越界值以及正常返回后的真实写入。
- 返回一个容器时，既要确认它能真正返回，也要确认同一路径上什么值仍留在容器里；嵌套观察记录原容器、存储位置和实际返回位置，仍调用同一存储追查，不新增第二张分析图。
- 普通函数、void 函数及无写入的函数都可能条件性抛出；后续写入须满足它实际正常返回的条件，前序写入不受后续异常抹除。
- 动态目标必须和实际接收对象的选择相符。声明范围已完整覆盖且只剩同一个实际实例时，无需重复枚举不影响行为的泛型实参；仍检查接收对象非空。
- 有限首轮路径可以提供真实写入见证；被截断的控制流回边或递归调用会保留不完整状态，没有正见证时不能据此发布 Getter。纯 finally 的正常返回查询使用实际 ret；catch/filter/fault 仍未完整处理。
- 公开可写字段可作为实际入口状态，Boolean 的 IL 范围为 0–255。私有、只读及未证明的状态不生成任意输入。正常入口不会假设此前有人用反射篡改 .NET 私有只读字段；当前实际到达的写入仍必须追踪并使相关初值证明失效。
- 物理事实按函数、基本块建立索引：同一函数是否有条件只检查一次，强转指令不再在每个位置扫描整个函数。公式环境和已完成 Setter 根的复用均限于当前运行阶段；恢复标签审计时不沿用冻结证明。
- 真实材料规模：144 份源码程序集、22,973 个源码文件、394 个外部引用、292,571 个源码函数；khengine 可报告函数 **15,051**，源码 NLT **3,272**，未标 NLT **11,779**。这些是输入统计，不是检测结论。
- 中途证据审计定位到 switch 条件缺失 598 个函数、初始化限制 541 个函数、字段初始状态限制 503 个函数。这些是当时失败证据命中数，不是修复后必然增加的结论数；等待调用的积压也不能全部算作功能缺口。实际 String.Replace 的 localloc 属于已有 IL 的指令支持不足，不是找不到原生源码。
- 材料范围复核：144 个编译节点对应 144 份不同参数文件，22,973 个源码输入无重复；游戏主程序集包含 khengine 接口的实际实现，不能直接删除。预编译 DLL/PDB 的全部依赖、编译选项和生成输入未验证，不能直接当作当前源码跳过编译。
- 当前 Editor 的 Mono 6.13 定制 DLL/PDB 配对已核实，但 PDB 中原生源码无校验内容，尚不能确定对应源码提交；本地 KiHan 的 Mono 5.0.1 不能替代实际运行实现。SVN 历史访问被拒绝，未绕过权限。
- 真实写法已有可定位复刻，包括 FindObjectFrame、GetRootFollowParent、RunNodeInvert、GetMoveToVkeys；循环多次创建对象、DictionaryEx 的 catch 返回、ConfigItem.ReleaseData 的跨入口 MethodInfo、AIService 原生效果仍需闭合。边界失败测试不等于能力完成。
- 审计只给所检查范围的结论；主 Agent 编写并执行全部测试。中途报告仅抽查过部分条目，未声称全部差异已正确。

### 旧版审计对照

以下是新版核查结果，不是最终 khengine 差异归因：

| 旧问题 | 新版复查 |
| --- | --- |
| NLT 只改自己显示，不影响上层 | 本批已修复；真实行为与标签生效后追踪分别保留，三种豁免、独立写入、跨程序集及进度对照通过 |
| Console/Math/Unity 整类硬编码效果 | 新版没有这张效果表；读取真实 IL，原生缺失仍报未完成，不能据此声称所有外部边界已解决 |
| 接口/虚函数永久开放、委托只看局部 | 使用真实继承与调用绑定，已有普通跨函数/对象字段委托对照；跨独立入口的注册表仍不完整 |
| 新外壳中的成员指向旧对象却漏报 | 原有源码/DLL 正反对照通过；不是把所有新容器的成员都视为新对象 |
| 第二轮循环才改旧对象 | 目前明确保留未证明，不再错报 Getter；但仍不能准确输出 Setter，属于能力缺口 |
| Pure 与未知委托合并时丢未知 | 精确双分支对照保留待处理；显式创建纯委托时不产生结论，不生成补丁 |
| 委托 y:/x: 实参写反 | 显式创建委托隔离编译器缓存后，源码/DLL 的旧对象和新对象两个方向对照通过 |
| 空 ref／空属性 setter 被当写入 | 两种原样对照在源码/DLL 中都得到 Getter，读取真实函数体而非仅看可写语法 |

补测试时还发现当前测试编译器会将方法组转换为私有静态委托缓存。`flag ? Pure : unknown` 的原样写法确实可能写 `<0>__Pure`，所以真实 Setter 有具体缓存证据，未知调用仍使整轮未完成；它不同于“把未知当 Setter”。另有仅使用静态缓存委托时尚未闭合的历史内容来源，属于跨入口状态缺口，不为编译器字段名增加特例。日志是否应豁免这种合理缓存留在最终业务复查中，不篡改真实写入事实。

### 仍未解决的关键问题

1. **完整静态初始化与反射**：正常入口约定已写入第二节。当前只证明可独立完成的初始化，并保留静态对象归属；普通调用、直接静态访问和反射静态读取都检查隐式初始化影响。更复杂的初始化、只读字段反射写入仍明确失败，不能把这次 List 用例通过宣传成全部运行库已闭合。
2. **动态选择的稳定性**：条件可能来自返回值、字段写回、循环和别的调用；确定目标之前必须使用同一份已闭合事实。完整反射、静态注册表和跨入口状态还没有达到全量验收。
3. **异常与原生实现**：完整异常关系仍未实现。当前 Editor 的 Mono 为 6.13.0，构建标记为 `2021.3.16f1.win.trunk.71`；KiHan 中候选源码配置为 5.0.1，不能当作实际实现。当前 DLL 的本地 SVN 最后变更为 r1099921；需要其 Mono 精确提交、补丁和构建配置，不能用官方版本族分支代替定制构建证据。
4. **实际耗时与规模**：泛型和不同调用环境的重复展开尚未解决。简化方案必须保留影响判断的参数差异，不得把不同对象或未确定调用混成同一结果。
5. **循环与完整执行路径**：条件失败已按位置区分，但循环回边、完整异常出口与跨入口状态仍须进一步复刻真实写法。没有进程崩溃、单个反例修复或整套单元测试通过，都不等于已经覆盖真实项目的全部写法。
6. **写入与路径的联合证明**：排除矛盾整数条件只是一步；对象或数值来自多个分支时，不能把成功分支和另一个分支的旧对象拼在一起。现有来源集合还需要保留实际选择关系，不能拿没有这些约束的公式“有解”作为 Setter 证据。仍不得宣称不存在误判。

### 交付与编码

新增、修改、生成的源码和文本配置统一使用 UTF-8 无 BOM，保留原换行，写入显式指定编码，交付前检查文件头没有 EF BB BF。二进制不进行文本编码转换。

报告、缓存、个人工具和临时项目不提交，KiHan 继续只读。失败报告不能冒充验收通过，也不为无法证明的函数生成标签修改。

### 保留的参考依据

- [Unity Mono 对应分支](https://github.com/Unity-Technologies/mono/blob/unity-2021.3-mbe/mono/metadata/assembly.c)：当前运行装载以实际宿主为准，不再使用不同代 IL2CPP 的版本表。
- [参考程序集不是运行实现](https://learn.microsoft.com/en-us/dotnet/standard/assembly/reference-assemblies)：不能用参考桩证明没有修改。
- [ILLink 的反射分析组织](https://github.com/dotnet/runtime/blob/main/src/tools/illink/src/linker/Linker.Dataflow/ReflectionMethodBodyScanner.cs)：可以参考对象、参数与统一操作的组织，不能照搬其未知值策略作为最终结论。
- [Z3 固定位宽整数说明](https://microsoft.github.io/z3guide/docs/theories/Bitvectors/)：表达 IL 整数位宽、普通回绕及有符号/无符号比较；公式求解不能代替缺失的对象和执行关系。
