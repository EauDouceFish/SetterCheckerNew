# SetterCheckerNew 设计与进度

## 一、最终目标

SetterCheckerNew 只服务于 `D:/KiHan/Packages/khengine`。

它读取 khengine 当前真正使用的 Unity 编译参数，检查所有相关源码和编译后的 C# 文件，最后回答：

1. 哪些可报告函数的真实行为是 Setter。
2. 哪些可报告函数的真实行为是 Getter。
3. 分析结论与源码现有标签有哪些差异。
4. 哪些人工豁免需要复查，哪些裸 `[NoLogTrack]` Setter 缺少 Reason。

工具生成文本报告、内容一致的 JSON 报告和标签修改预览，不自动修改 KiHan。

## 二、已经确认的判断规则

真实行为只分 Getter 与 Setter。以下行为属于 Setter：

- 修改当前对象、传入对象、对象图、静态数据、集合、数组、事件或 `ref/out` 数据。
- 调用任意一个 Setter。
- 创建 khengine 业务对象并将它返回、保存、注册或传给外部。

只修改函数内部创建且没有离开函数的临时对象，不属于 Setter。

只有函数自身没有外部修改，并且全部合法调用目标都是 Getter，函数才是 Getter。不能闭合时停止分析并报告准确位置，禁止猜测、保守判定或继续生成不完整结论。

`ShouldTrack`、`NoLogTrack` 和 `NLTClass` 是第六模块结合真实行为与源码标签后得出的最终日志决定，不是函数真实行为的别名。`[LogTrack]` 只强制最终决定为 `ShouldTrack`，不能把真实 Getter 改写成 Setter。

## 三、现有标签规则

- 方法级 `[NoLogTrack(Reason)]`、裸 `[NoLogTrack]` 和类级 `[NoLogTrack]` 都先作为可信人工豁免，最终不追踪。
- 工具仍独立检查所有豁免函数的真实行为。
- 类级豁免下的可报告函数标记为 `NLTClass`。
- 带 Reason 的 Setter 和类级豁免 Setter 进入“可信豁免复查”，不报警。
- 只有裸 `[NoLogTrack]` 被独立分析为 Setter 时，告警“缺少 Reason”。
- 该告警传到所有能调用它的上层可报告函数；每个函数只列一次最短调用过程。
- 没有 NLT、但实际是 Getter 的函数进入补标建议。
- 同一函数或其所属类同时出现互相冲突的 NLT 与 `[LogTrack]` 时，正式分析失败并指出冲突位置，不猜测哪一个优先。

现有标签不作为语义分析的答案，只影响最后的日志决定和差异分类。

## 四、统计范围

“可报告函数”指进入最终数量统计和标签差异的函数，包括：

- 普通命名函数。
- 运算符和类型转换函数。
- 属性 setter。
- 自定义事件 add/remove。
- 类级 NLT 下原本可报告的上述函数。

构造函数、属性 getter、接口或抽象声明、局部函数、匿名函数、编译器生成函数、`getInstance` 和 `BaseProxy_*` 不进入标签统计，但仍参与调用分析。

## 五、七个模块

生产代码按以下七个模块组织，一个模块一份主文件。主文件可以包含本模块紧密相关的小类型，不再按类拆文件。

### 1. `MaterialLoader.cs`：找齐材料

- 找到 `khengine.runtime.asmdef` 当前唯一的 Unity 编译参数文件。
- 读取实际源码、条件编译标记、分析器引用、源码依赖和外部编译文件。
- 递归读取依赖的 Unity 源码程序集，但只有 khengine 进入最终统计。
- 保留 Unity 响应文件中的完整编译选项、分析器和源码生成器；生成的源码与普通源码一起进入材料。
- 参考文件以及类型转交目标映射到同一 Unity/.NET 环境中的真实承载文件。
- 找不到、找到多份或身份不一致时直接失败并列出证据。

### 2. `MethodCatalog.cs`：建立函数总表

- 为每个源码函数建立唯一身份；托管文件的函数以“真实文件 + 元数据标记”天然唯一，在实际调用或候选类型需要时读取并在本次运行内保存，避免启动时创建几十万个无关对象。
- 记录类、函数、参数、文件、行号、标签和是否进入最终统计。
- 预先建立全部类型的继承与接口实现索引，以及源码函数的重写与成员名称索引；外部候选类型一旦被命中，就从同一真实文件读取该类型的函数，函数自身携带参数、重写目标和实现目标供后续模块使用。

### 3. `BehaviorReader.cs`：读取函数行为

- 源码通过 C# 编译器读取。
- 真实托管动态链接库直接读取函数指令。
- 两者统一记录字段写入、属性写入、数组或集合写入、事件修改、`ref/out` 修改、调用、对象创建、返回、保存、注册、传出和对象来源。
- 这一模块只描述事实，不直接给出 Getter、Setter 或标签结论。

### 4. `CallTargetResolver.cs`：找齐调用目标

- 普通调用连接到明确函数。
- 接口调用连接到全部合法实现。
- 可重写函数连接到全部合法重写。
- 委托连接到全部可能绑定的函数。
- 事件回调与委托使用同一套绑定目标规则。
- 反射按类型、名称和参数解析字段、属性、函数与构造函数。
- `GetMethod("Foo")` 只匹配合法的 `Foo`；名称来自参数时追踪全部调用点传值。
- 反射名称允许由外部任意传入时，检查当前代码闭包内所有类型和签名能够匹配的成员。
- `FieldInfo.SetValue` 和 `PropertyInfo.SetValue` 产生普通写入关系，不硬编码最终结论。

### 5. `EffectAnalyzer.cs`：得出真实结论

- 先标出直接修改外部状态的函数。
- 再沿调用关系向上传播 Setter。
- 跟踪新对象是否只在函数内部使用，还是被返回、保存、注册或传出。
- 循环调用使用同一轮稳定计算，不重复扫描全部函数。
- 全部关系闭合后，每个函数的真实行为只有 Getter 或 Setter 两种结论。

### 6. `AnnotationEvaluator.cs`：处理标签和差异

- 应用可信 NLT 与 `[LogTrack]` 规则。
- 生成可信豁免复查、裸 NLT 缺少 Reason 告警、上游调用过程和可补标清单。
- 只在这一模块处理标签，不改变前五个模块的真实行为分析。

### 7. `ReportWriter.cs`：生成交付结果

- 输出总函数数、检测结果数量和源码标签数量。
- 按类汇总两类主要差异。
- 输出可信豁免复查、缺少 Reason、失败证据和每阶段耗时。
- 从同一结果生成文本、JSON 与补丁预览，顺序固定。

## 六、代码目录

仓库只保留三个项目：

```text
Src/SetterChecker.Core/
  SetterChecker.cs              总入口、共享数据和七个模块的顺序
  MaterialLoader.cs
  MethodCatalog.cs
  BehaviorReader.cs
  CallTargetResolver.cs
  EffectAnalyzer.cs
  AnnotationEvaluator.cs
  ReportWriter.cs
  SetterChecker.Core.csproj

Src/SetterChecker.Cli/
  Program.cs
  SetterChecker.Cli.csproj

Tests/SetterChecker.Core.Tests/
  MaterialLoaderTests.cs
  MethodCatalogTests.cs
  BehaviorReaderTests.cs
  CallTargetResolverTests.cs
  EffectAnalyzerTests.cs
  AnnotationEvaluatorTests.cs
  ReportWriterTests.cs
  TestProject.cs
  SetterChecker.Core.Tests.csproj
```

不建立空目录、插件系统、依赖注入容器、适配层、一类一文件结构或未来需求占位。生产入口不支持任意普通 `.csproj` 或通用 NuGet 工程；自动化测试仍可建立最小 C# 夹具验证源码与动态链接库的同一写法。`bin/Debug`、`bin/Release`、`obj`、`artifacts` 和 `TestResults` 都是临时生成内容，不属于源码，不提交。

手写生产代码以 3000–5000 行为目标。不能为了压行数牺牲可读性，也不能靠继续拆文件掩盖复杂度；预计超过 5000 行时必须先说明不可省略的原因。

模块 2 完成时手写生产代码为 4,981 行。其精确处理源码与真实动态链接库身份、泛型、接口、重写、类型转交和元数据边界的代码均由测试及真实缺陷推动，不能删除；其余五个模块也属于已确认目标，因此开始模块 3 前已经向用户说明总量必然超过 5,000 行。后续继续以简单和可读为约束，逐模块报告新增行数，不把总行数当作牺牲正确性的硬上限。

每个函数的 XML 文档块之前写一行简短中文 `//` 注释，直接说明这个函数的用途；没有 XML 文档时注释紧贴函数声明。公开函数仍按统一编码规范保留必要的 XML 文档，但不写重复的大段说明。

## 七、外部代码与动态调用

- 当前 Unity 编译参数列出的源码和托管动态链接库都必须读取真实内容。
- 只有声明的参考文件必须映射到当前运行环境真实文件后再分析。
- 包装函数通过自己的源码或函数指令自然进入调用关系，不为 KFL、ReflectUtil 或具体业务函数写特例。
- 接口、重写、委托和反射统一遵守“全部合法目标都闭合”的规则。
- `Activator.CreateInstance` 解析传入类型及构造函数，并继续判断新对象是否离开函数。
- 第一轮不使用 LogicData 限制反射目标，不枚举未启用的条件编译组合。
- 如果某个可报告函数的结论依赖原生函数体，正式分析失败并指出调用过程；不影响任何可报告结论的原生边界才进入延期清单。
- 运行时生成新代码或加载当前材料范围外的程序集时直接失败，不提供兜底结果。

## 八、禁止的实现方式

- 禁止按某个业务函数名、类名或报告差异硬编码 Getter、ShouldTrack 或忽略结论。
- 禁止 `Unresolved => ShouldTrack`、`Unresolved => NoLogTrack` 或任何保守兜底。
- 禁止捕获所有异常后继续分析、缺文件时扫描全盘、按相似文件名猜实现。
- 禁止为未出现的输入格式、旧版本、插件能力或跨运行缓存预建抽象。
- 禁止为了“职责纯洁”把一段连续流程拆成大量只转发一次的小类。
- 必需的路径、文件、程序集身份和目标闭合校验不属于防御性编程，必须保留；禁止的是猜测、兜底和额外替代路径。

## 九、多线程和性能

命令行接受 `-j4`、`-j 4` 和 `--jobs 4`。数值是整个工具共用的最大并行工作数，不写死为 4。

默认工作数为 4，任意正整数有效，0 或负数直接拒绝。源码文件、外部文件、函数行为和动态目标等互不依赖的项目使用同一个并行上限。存在明确先后关系的七个模块按顺序执行。

`-j1` 在取得编译内容前关闭编译器内部并行；同时处理多个源码程序集时也关闭每份编译内容的内部并行，避免外层与内层工作队列叠加突破全局上限。只有当前步骤只处理一份源码程序集时，才允许编译器在 `-j` 大于 1 时使用内部并行。每一步结束后固定排序，因此 `-j1`、`-j2`、`-j4`、`-j8` 必须得到逐字一致的结果。

允许一次运行内使用函数、类型、程序集和调用关系字典，不使用持久化缓存。完整分析目标是 5 秒以内，连同工具编译最低要求是 10 秒以内；每个模块单独计时。

## 十、测试和自动审计

测试只通过模块公开结果或工具总入口验证行为，不检查内部拆了多少函数、调用了几次私有方法。每增加一种能力执行一次“先失败、再实现、再整理”的小循环。

同一个最小函数必须分别从源码和真实托管动态链接库读取，两种来源得到的写入、调用、创建、对象来源和最终真实行为必须一致。

每个模块必须经过：

1. 用明确预期的小例子覆盖正常情况和当前项目实际边界。
2. 编译、格式检查和全部自动化测试通过。
3. 在 KiHan 中只读查找本模块涉及的真实写法，并建立一一对应的最小测试。
4. 把代码、测试和本文件交给子 Agent 审计。
5. 审计不通过就自动修复并重新送审，直到明确通过；只有审计通过后才提交和推送。若修复需要新增语义选择或扩大范围，则停止并询问用户。
6. 等待审计期间继续做不依赖审计结论的检查、真实写法整理和后续测试资料准备，不空等；下一模块实现仍在当前模块通过后开始。
7. 通过后检查完整差异，提交并推送可回滚版本。

最终再用 `D:/KiHan/Packages/khengine/Runtime/khengine.runtime.asmdef` 完整运行：

- 每种真实写法都有对应测试。
- 除确认不影响任何可报告函数结论的原生边界外没有无法闭合项。
- 工具结论和源码标签的每项差异都由 AI 复查归因。
- `-j1`、`-j2`、`-j4` 结果一致，并记录性能。

## 十一、执行代办清单

当前总进度：**2 / 7 个模块完成**。模块只有在测试、khengine 只读验收、子 Agent 审计、修复复审、提交和推送全部完成后才计入完成数。

- [x] 重新汇总此前确认的 Getter、Setter、动态链接库、反射、委托、NLT、并行和报告规则。
- [x] 收缩为七个模块、每个模块一份主文件。
- [x] 将 3000–5000 行、函数头中文注释、禁止兜底和禁止过度设计写入项目规则。
- [x] 删除 `artifacts`、`TestResults`、`bin/obj`、个人编辑器文件和空文档目录。
- [x] 完成七模块收缩方案的三轮子 Agent 设计审计；前两轮问题已修复，第三轮结果为 PASS。
- [x] **模块 1：找齐材料。**
  - [x] 测试先行并完成实现。
  - [x] khengine 只读验收和 `-j` 一致性验证。
  - [x] 子 Agent 审计；第一轮问题已修复，第二轮为 PASS。
  - [x] 完整差异、编译和测试通过。
  - [x] 已提交并推送：`04c9546`。
- [x] **模块 2：建立函数总表。**
  - [x] 建立源码函数、参数、位置和标签清单。
  - [x] 按真实文件和元数据位置读取托管动态链接库函数，并按命中类型延迟展开无关函数。
  - [x] 建立继承、接口、重写和成员名称索引。
  - [x] 验证哪些 khengine 函数进入最终统计。
  - [x] 验证不同 `-j` 的清单、顺序和数量一致。
  - [x] 本模块自动化测试已写完并曾全部通过；提交前仍会完整复跑。
  - [x] 本地代码规范、Release 编译、18/18 测试和差异复核通过。
  - [x] 第一轮验收时，khengine 的 `-j1`、`-j2`、`-j4` 均得到 50,368 个源码函数、57,509 个类型和 15,230 个可报告函数。
  - [x] 子 Agent 第一轮审计为 REJECT：发现外部函数关系、继承接口索引、原生声明统计和代码格式问题。
  - [x] 已补测试并修复外部函数重写、隐式接口、泛型显式接口和继承接口索引。
  - [x] 已让 33 个原生声明函数进入统计；khengine 可报告函数因此由 15,230 个变为 15,263 个。
  - [x] 已修复主类型顺序、重复基础类型名称和混合换行问题。
  - [x] 修复后 Release 编译、18/18 测试、格式检查和 khengine 三种并行度复测通过。
  - [x] 第二轮子 Agent 审计为 REJECT：发现门面类型无法映射真实类型，以及继承接口上的 `new virtual` 假关系。
  - [x] 已建立参考文件类型到真实类型的统一别名，并用真实文件位置读取函数。
  - [x] 已区分当前类直接声明的接口和从基类继承的接口，消除 `new virtual` 假关系。
  - [x] 已把反复传递的继承关系字段收成模块内一个小记录，解决第二轮代码结构意见。
  - [x] 修复后 Release 编译、19/19 测试和 khengine `-j4` 复测通过。
  - [x] 第三轮子 Agent 审计为 REJECT：门面身份未覆盖函数签名、直接子接口漏父接口函数、同名类型可能跨文件错绑。
  - [x] 已统一替换函数参数和返回值中的门面类型身份，并向直接子接口的父接口传播直接实现关系。
  - [x] 已按每个真实文件明确记录的类型定义和逐类型转交目标解析别名，不再按整个转交闭包只看同名。
  - [x] 已增加“门面自己定义类型、目标文件恰好同名但没有转交”的反例测试。
  - [x] 修复后 Release 编译、19/19 测试、格式检查和 khengine `-j4` 复测通过。
  - [x] 第四轮子 Agent 审计为 REJECT：一个带 `<` 的编译器生成类型名被误当成泛型语法；歧义类型身份被静默跳过；完整扫描没有稳定保持在 10 秒内。
  - [x] 已用失败测试复现并修正编译器类型名解析；同一参考类型对应多个真实类型时会列出全部候选并停止。
  - [x] 已让外部类型读取同时保存后续需要的元数据，并按源码类型和源码文件共享 `-jN` 工作队列，避免重复读取和大程序集各占一个线程。
  - [x] 修复后 Release 编译、20/20 测试通过；khengine 的 `-j1`、`-j2`、`-j4` 数量一致，连续两次 `-j4` 完整运行约 9.8 秒和 9.2 秒。
  - [x] 第五轮子 Agent 审计确认 BouncyCastle 2,639 个类型、22,324 个函数全部展开，完整索引在三种并行度下逐项一致。
  - [x] 第五轮子 Agent 审计为 REJECT：源码嵌套泛型身份漏掉外层泛型实参；一处路径比较违反编码规范；另有一个字符串拼接键和一个已无用途字段需要收净。
  - [x] 已补嵌套泛型源码/动态链接库同一性测试，并修复第五轮的四项问题。
  - [x] 已让外部材料的独立文件查找和转交链读取共享 `-jN` 上限；连续两次 `-j4` 完整运行约 9.6 秒和 9.8 秒。
  - [x] 第六轮 Standards 为 PASS，性能无竞争复测约 9.0 秒和 8.5 秒；Spec 为 REJECT：开放泛型在源码侧缺少元数据占位实参。
  - [x] 已分开“类型定义身份”和“函数签名类型身份”，并补顶层、两层、三层及只保留外层参数的源码/动态链接库同一性测试。
  - [x] 第七轮 Standards 与性能通过，开放泛型全部通过；Spec 为 REJECT：源码隐式接口实现没有像动态链接库一样记录接口函数目标。
  - [x] 已补源码/动态链接库隐式接口关系同一性测试，并用编译器的真实接口实现结果统一关系记录。
  - [x] 修复后 Release 编译、20/20 测试通过；连续两次 `-j4` 完整运行约 9.1 秒和 9.0 秒。
  - [x] 第八轮 Standards、性能和接口关系通过；Spec 为 REJECT：动态链接库的显式接口属性/事件访问器被归为普通函数。
  - [x] 已补显式接口属性/事件源码与动态链接库同一性测试，并改为读取属性/事件访问器元数据。
  - [x] 已增加名称类似 `get_` 但实际是普通函数的反例，避免继续按名称猜测函数种类。
  - [x] 修复后 Release 编译、20/20 测试通过；最新 `-j4` 完整运行约 10.0 秒。
  - [x] 第九轮 Standards、性能和访问器矩阵通过；Spec 为 REJECT：`checked explicit` 转换在动态链接库侧被归为普通运算符。
  - [x] 已补普通/checked 转换的源码与动态链接库同一性测试，并识别标准 checked 转换元数据名称。
  - [x] 修复后 Release 编译、20/20 测试通过；最新 `-j4` 完整运行约 9.8 秒。
  - [x] 第十轮 checked 转换及全部功能矩阵通过；审计为 REJECT：6 个内部类型和 5 个公开测试辅助函数缺少 XML 说明，并多识别了非标准 `op_CheckedImplicit`。
  - [x] 已补齐函数总表全部内部小类型及本模块新增公开测试辅助函数的 XML 说明。
  - [x] 已移除非标准 `op_CheckedImplicit`，仅按标准名称识别 checked 显式转换。
  - [x] 修复后 Release 编译、20/20 测试通过。
  - [x] 第十一轮 Spec 全部通过；Standards 为 REJECT：当前差异中仍有 19 个公开声明缺少 XML 说明。
  - [x] 已一次补齐语法树全量扫描列出的 19 个 XML 说明。
  - [x] 第十二轮 Standards 通过；Spec 为 REJECT：源码遗漏引用返回和返回自定义修饰符，导致同一函数与动态链接库身份不同。
  - [x] 已用失败测试复现 `ref` 返回、`ref readonly` 返回和 `init` 属性，并统一按托管元数据顺序生成源码返回签名。
  - [x] 第十三轮 Standards 通过；Spec 为 REJECT：多个返回修饰符的顺序相反，参数修饰符尚未进入身份。
  - [x] 已用真实动态链接库双修饰符测试复现返回、参数和重写关系差异，并按元数据嵌套顺序统一修复。
  - [x] 已补只读引用参数测试，并修复带修饰符引用参数的传递种类读取。
  - [x] 第十四轮 Standards 通过；Spec 为 REJECT：引用与普通修饰符组间顺序错误，动态链接库的 `in` 参数隐式接口关系丢失。
  - [x] 已用两组修饰符同时存在的真实动态链接库测试复现并修正“类型修饰符、引用符号、引用修饰符”的结构顺序。
  - [x] 已补源码/动态链接库 `in` 参数隐式接口关系测试；接口匹配忽略不参与匹配的修饰符但严格保留 `ref/out/in` 种类。
  - [x] 已移除隐式接口实现必须是虚函数的错误限制，并拒绝同名静态函数。
  - [x] 第十五轮 Standards 通过；Spec 为 REJECT：用正则删除修饰符会误读合法括号或伪装成修饰符的真实类型名。
  - [x] 已删除修饰符正则；源码与托管类型在读取时同时生成完整身份和不含真实修饰符节点的接口匹配身份。
  - [x] 已补右括号类型名、函数指针修饰符和名称形似 `modopt(...)` 的普通类型反例，防止关系漏报与误报。
  - [x] 第十六轮 Standards 通过；Spec 为 REJECT：嵌套修饰信息、泛型接口实参、函数指针实参和调用约定还有四处身份问题。
  - [x] 继承和接口关系改为直接保存类型定义、完整实参和匹配实参，不再从身份文字中反解析泛型参数。
  - [x] 源码与托管函数指针统一记录调用约定、扩展非托管约定和泛型参数数量。
  - [x] 已补嵌套修饰泛型、双参数函数指针泛型接口、调用约定与函数指针泛型元数测试。
  - [x] 已改用 .NET 自带元数据读取器读取生产动态链接库；第三方库仅用于测试生成边界样本。
  - [x] 已补程序集名称大小写、大小写碰撞顺序、伪模块类型、合法同名类型和反引号类型名回归。
  - [x] 已让显式实现体同时支持直接函数定义、当前类型函数引用和逐位使用自身参数的泛型类型引用，并用运行时可装载样本验证静态/实例同签名与外部父类型拒绝规则。
  - [x] Release 编译和 30/30 测试通过。
  - [x] khengine 全量结果为 50,368 个源码函数、87,901 个类型、15,263 个可报告函数，直接继承与接口关系 0 条未闭合。
  - [x] `-j1/-j2/-j4/-j8` 结果完全一致；优化后本轮耗时约 11.214、8.232、6.039、5.215 秒。
  - [x] 最近一轮终审发现一个当前新增公开测试辅助函数缺少 XML 说明；已连同相邻同类函数一起补齐。
  - [x] 复用不可变元数据参考，并关闭不适合一次性分析工具的分层执行；三次独立 `-j4` 完整分析墙钟为 6.028–6.250 秒。
  - [x] 清理 Release 输出后重新编译并完整分析两次，端到端墙钟为 8.220 秒和 8.271 秒。
  - [x] 最终双路子 Agent 审计结果均为 Standards PASS、Spec PASS。
  - [x] 最终 khengine 验收完成，直接继承与接口关系没有未闭合项。
  - [x] 已提交并推送：`fd6b6e4`。
- [ ] **进行中：模块 3：读取函数行为。**
  - [ ] 测试设计与失败用例。
  - [ ] 实现源码与托管动态链接库的统一行为读取。
  - [ ] khengine 真实写法复刻和只读验收。
  - [ ] 子 Agent 审计、修复和复审。
  - [ ] 提交并推送。
- [ ] **模块 4：找齐调用目标。**
  - [ ] 测试设计与失败用例。
  - [ ] 实现普通调用、接口、重写、委托、事件和反射目标解析。
  - [ ] khengine 真实写法复刻和只读验收。
  - [ ] 子 Agent 审计、修复和复审。
  - [ ] 提交并推送。
- [ ] **模块 5：得出 Getter/Setter 真实结论。**
  - [ ] 测试设计与失败用例。
  - [ ] 实现直接修改、调用传播、新对象逃逸和循环调用分析。
  - [ ] khengine 真实写法复刻和只读验收。
  - [ ] 子 Agent 审计、修复和复审。
  - [ ] 提交并推送。
- [ ] **模块 6：处理标签和差异。**
  - [ ] 测试设计与失败用例。
  - [ ] 实现 NLT、NLTClass、Reason、LogTrack 和差异规则。
  - [ ] khengine 真实标签复刻和只读验收。
  - [ ] 子 Agent 审计、修复和复审。
  - [ ] 提交并推送。
- [ ] **模块 7：生成报告。**
  - [ ] 测试设计与失败用例。
  - [ ] 实现文本、JSON 和标签补丁预览。
  - [ ] khengine 完整报告验收。
  - [ ] 子 Agent 审计、修复和复审。
  - [ ] 提交并推送。
- [ ] 最终一一复刻 khengine 真实写法并逐项审查差异。
- [ ] 完成 `-j1`、`-j2`、`-j4` 结果一致性与性能验收。
- [ ] 完整版本提交并推送。
