# SetterCheckerNew 项目规则

## 目标与边界

- 完整目标、判断规则、模块组织和进度以 `Docs/PROJECT-PLAN.md` 为准，修改实现前必须先读取。
- 工具只对 `D:/KiHan/Packages/khengine` 输出结论；`D:/KiHan` 只读，除非用户另行明确授权。
- 使用 `D:/KiHan/Packages/khengine/Runtime/khengine.runtime.asmdef` 当前真实 Unity 编译参数，不枚举其他条件编译组合。
- 不使用 LogicData 限制合法输入。按最新要求，不再索取缺失的原生源码或开发原生指令分析器；现有材料不能证明的原生影响必须单列，不得猜成 Getter 或 Setter，也不得掩盖托管分析缺陷。

## 简单实现

- 保留 Core、Cli、Tests 三个项目。
- 七个业务模块各一份主文件；紧密相关的小类型留在模块文件内，禁止恢复一类一文件。
- 允许模块内部适度耦合。只有真实存在的第二种实现才建立接口，禁止依赖注入容器、插件框架、通用仓储、空适配层和占位抽象。
- 手写生产代码优先目标 3000–5000 行；Core 尽量不超过 13,000 个物理行，包含注释和空行，排除 bin/obj。本次不以历史 16,000 行例外作为继续增长的授权；超额须先向用户说明，不得由 Agent 自行放宽。禁止删功能、拆出 Core 或压缩排版绕过额度。
- 现有实现曾获限定额度例外，仍以 13,000 行为优先目标；例外不代表架构没有精简空间，也不代表功能或 khengine 验收通过。
- 不建立空目录；不保留或提交 `artifacts`、`TestResults`、`bin`、`obj`、个人编辑器文件或临时探针项目。
- 禁止防御性编程和兜底策略：不猜文件、不扫描全盘、不吞错后当作成功、不为尚未出现的输入增加替代路径。必需的路径、文件、程序集身份和目标闭合校验必须保留。明确的分析失败可以保留为报告证据并继续检查独立项目，但不得为失败函数生成最终决定，整轮验收仍失败；程序缺陷不得用捕获所有异常掩盖。
- 禁止针对单个业务函数、类或报告差异硬编码结论。反射、委托、接口、重写、源码和 IL 必须进入统一语义流程。
- 已确认的轻量主干：每个函数只保存一份修改说明；保留固定调用关系和参数对应表，不以调用者、入口或调用路径复制函数分析环境。
- 源码直接由 Roslyn 读取，不为分析生成内存或磁盘程序集；源码项目之间用 CompilationReference，真实外部 DLL 才读取 IL。不同输入共用修改说明，不强行共用指令读取。
- 每份当前函数体只提取一次事实；随后只在说明新增时通知受影响调用者，不重跑全部函数、不恢复第二轮全调用展开。递归通过有限说明传递处理，禁止展开调用路径。
- Setter 判断不回退；修改对象、参数及返回来源的说明仍须完整，不得只缓存一个布尔值而丢掉调用者需要的信息。
- 不再把整条调用链的执行条件、异常、类型初始化和正常返回证明作为默认工作流。必要的函数内对象来源和明确不可达代码检查保留；未覆盖项明确说明，不为单个案例恢复全局执行推算。

## 语义与标签

- 真实行为只分 Getter 与 Setter；最终日志决定只分 ShouldTrack、NoLogTrack 与 NLTClass，禁止混用。
- 修改已有对象、参数对象图、静态状态、集合、事件或 `ref/out` 数据为 Setter；调用影响按实际接收对象和参数传播，不能把被调函数的 Setter 结论无条件复制到上层。
- 创建并返回、保存、注册或传出 khengine 业务对象为 Setter；只修改不逃逸的内部新对象不算。
- 只有全部合法目标闭合且没有外部效果才是 Getter。禁止把 Unresolved 映射为任何最终结论。
- `[LogTrack]` 只强制最终 ShouldTrack，不改变真实行为；互斥日志标签同时出现时正式分析失败。
- 方法级带 Reason NLT、裸 NLT、类级 NLT 都是可信豁免，但仍独立审计真实行为。
- 可信 NLT 内部的写入不再使上层需要追踪；调用者自己的写入、保存或返回业务对象仍独立判断。标签不能改变真实参数、返回对象、字段写回或能否正常返回；真实分析与标签生效后的追踪决定保留不同证据。
- 裸 NLT 仅在真实行为为 Setter 时告警缺少 Reason，并把最短调用过程传播到上游可报告函数。
- 可报告范围和完整标签规则见设计文档，不得用源码标签代替分析真值。

## 多线程与性能

- 支持 `-j4`、`-j 4`、`--jobs 4` 和任意正整数；默认 4，全程序共用一个最大并行数。
- 同一输入在 `-j1`、`-j2`、`-j4`、`-j8` 下结论、证据和顺序必须一致；耗时与缓存命中计数单独比较。
- 按 `-j` 读取独立源码、提取函数事实及读取 DLL，共用一个并行额度；不再调度游戏程序集生成任务。
- 允许会话内复用源码编译上下文及持久化缓存。缓存必须核对实际源码、配置、引用、生成源码及分析规则；文件日期或同名 DLL 不足以证明有效。修改后不得沿用旧对象编号和结论。
- 完整分析目标 5 秒以内，包含编译最低要求 10 秒以内；输出每模块耗时。
- 每次主干改动必须以真实 khengine 的一次完整运行及报告验收；报告写出计入耗时，不能用局部测试替代。当前批次只做一轮完整验收，失败后先汇报整体瓶颈，不边跑边补特例、反复扫描。
- 验收同时记录不同函数数、函数体提取次数、固定调用关系数及说明更新次数；源码程序集生成数必须为零，函数体提取次数不得超过实际分析的不同函数数。

## 测试、审计与版本

- 每个模块按测试先行的纵向小循环开发，测试通过公开结果，不绑定内部拆分。
- 每个模块都要从 khengine 找出对应真实写法，建立一一对应的最小测试。
- 按用户最新要求，全部组织、实现、测试执行和审查均由主 Agent 亲自完成；不再启动或使用子 Agent。
- 每个模块完成后由主 Agent 对照设计、规范、测试和 khengine 真实输入审查。
- 每个模块开工前，先说明对应 khengine 需求、具体方案、现有代码如何复用和精简、暂不覆盖项及测试和性能标准，与用户讨论明确后再实施；不得自动开始下一模块。
- 近期先交付能扫描 khengine、列出已证明差异与未覆盖情况的版本，再与用户逐类讨论。禁止为了穷尽边界无限扩展本轮任务，也不能用阶段报告冒充全部通过。
- 已确认范围内，主 Agent 先完成整条最小工作流再验收；不以边界补丁无限扩大本轮任务。接口、委托与反射共用真实目标列表，函数不变事实只读取一次；仍须保留实际接收对象、参数和目标完整性。
- 每个通过审计的可运行版本，提交前检查完整差异并运行格式、编译和全部测试，然后提交并推送到 `origin` 当前分支。
- 不得提交报告、缓存、个人工具、临时项目或 KiHan 文件。推送失败必须报告本地提交号和原因。
- 每次准备结束回复前由主 Agent 检查当前请求是否完成、原始目标是否被偷换。等待用户确认模块方案或按要求提交阶段反馈时可以结束本轮，不表示整个工具完成；不得以旧“持续工作”规则越过开工前确认。
- README 只写用途、运行方式和当前限制；PROJECT-PLAN 只写需求、模块、当前状态和待办，不追加逐次修复、历史扫描或试验流水。运行日志代码的调整同样先明确方案。

## 编码规范与 SHA-256

- 新建、修改和生成的源代码与文本配置统一使用 UTF-8 无 BOM，保留原换行；写入显式指定无 BOM 编码，交付前检查文件头不存在 EF BB BF。二进制不转换编码。
- 遵循 `C:/Users/doucechen/Desktop/Personal/我维护的/规范/csharp编码规范/README.md`。
- 每个函数的 XML 文档块之前写一行简短中文 `//` 注释，说明函数用途；公开函数的 XML 文档必须紧贴函数声明。
- 禁止新增、实现或改写任何 SHA-256、SHA256、sha256、HMAC-SHA256 代码或封装；可以使用和检查已有实现。
