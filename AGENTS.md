# SetterCheckerNew 项目规则

## 目标与边界

- SetterCheckerNew 分析函数在全部合法输入下是否具备修改可观察战斗状态的能力，而不是模拟某一批部署数据形成的具体执行实例。
- 当前只对 `D:/KiHan/Packages/khengine` 输出统计、标签差异和补丁预览。
- `D:/KiHan` 仅用于只读分析和验收，除非用户另行明确授权，不得修改其中任何文件。
- 第一版使用 `D:/KiHan/khengine.runtime.csproj` 当前配置产生的 Compilation，不枚举其他条件编译组合。
- 第一版不读取 `LogicData`，不实现 KFL 专用数据规则。反射按代码闭包中的全部合法目标分析。
- 第一版不处理原生函数体；发现原生边界时记录为明确延期项。除明确延期边界外，任何无法闭合的分析都必须使验收失败。

## 语义规则

- 修改已有对象、静态状态、参数对象、参数对象图、集合内容、事件或 `ref/out` 数据的函数是 `ShouldTrack`。
- 调用任意一个 `ShouldTrack` 目标的函数也是 `ShouldTrack`。
- 创建并返回、发布、注册或写入 khengine 业务对象属于 `ShouldTrack`。
- 只修改函数内部创建且不会逃逸的临时对象，不属于 `ShouldTrack`。
- 只有全部调用目标都已闭合且没有上述效果，函数才是 `NoLogTrack` 候选。
- `Unresolved` 不是允许输出的最终分类。分析失败必须附带可定位的证据链。
- 接口、virtual、delegate 和反射调用使用同一原则：只有全部合法目标都是 Getter，调用才是 Getter。

## NoLogTrack 规则

- 现有方法级 `[NoLogTrack(Reason)]`、裸 `[NoLogTrack]` 和类级 `[NoLogTrack]` 都是可信豁免，在最终传播图中阻断 Setter 效果。
- 所有可信豁免函数仍执行独立语义审计，原始结论进入“可信豁免审计”报告，但不改变最终豁免结果。
- 类级 `[NoLogTrack]` 下的可报告函数标记为 `NLTClass`。
- 只有当裸 `[NoLogTrack]` 被独立语义审计判为 Setter 时，才产生“缺少 Reason”告警。
- 上述告警传播到所有可到达的上游可报告函数。每个受影响函数只记录一次，并附一条最短调用链，不枚举重复路径。
- 带 Reason 的方法级豁免和类级豁免不产生“缺少 Reason”告警。

## 禁止特例化

- 禁止针对单个业务函数、单个函数全名、单个报告差异直接配置 Getter、ShouldTrack、忽略或效果结论。
- 所有检测必须属于统一语义工作流。源码、IL 和运行时机制最终只能产生通用的读取、写入、调用、创建、对象来源和动态目标关系。
- 运行时机制规则必须覆盖同类操作的完整语义范围，并根据参数和目标产生普通语义边，不得直接返回最终 Getter 或 ShouldTrack 结论。
- 自定义包装函数必须通过分析其源码或 IL 自然进入统一工作流，不得为包装函数增加专用规则。
- 新增任何机制规则时，必须同时添加覆盖其正常情况和边界情况的测试。

## 可报告函数

- 最终统计包含普通命名方法、运算符、转换方法、属性 setter、自定义事件 add/remove，以及类级 NLT 下的这些函数。
- 构造函数、C# 属性 getter、abstract/接口声明、local function、lambda、编译器生成函数、`getInstance` 和 `BaseProxy_*` 不进入最终标签统计。
- 不进入统计不代表不分析；上述函数仍可作为调用图节点影响可报告函数。

## 架构与性能

- 使用三个项目：`SetterChecker.Core`、`SetterChecker.Cli`、`SetterChecker.Core.Tests`。
- 只为真实变化点定义接口，不引入依赖注入容器、通用仓储、插件框架或无实际需求的抽象层。
- 允许并要求使用单次运行内的 Method、Type、程序集和调用边字典索引。
- 命令行必须支持与构建工具 `-j4` 相同含义的 `-j N` 工作数量参数，同时接受 `-j4`、`-j 4` 和 `--jobs 4`。未填写时默认使用 4。
- 工作数量不得在代码中固定为 4；用户可以传入任意正整数。全程序共用同一个工作数量设置，禁止每个步骤各自再创建一组不受控制的线程。
- 源码文件、外部编译文件、函数内容和动态调用目标中互不依赖的工作必须使用该参数并行处理。存在先后依赖的汇总步骤不得为了表面并行而增加复杂度。
- `-j1`、`-j2`、`-j4`、`-j8` 对同一输入必须产生完全相同且顺序一致的报告。
- 禁止持久化增量缓存、旧版本兼容模式和兼容性报告。
- 冷启动分析目标为 5 秒以内；包含工具编译时总耗时 10 秒以内为最低可接受目标。每个阶段必须输出耗时。

## 开发与验收

- 先维护架构设计，再逐个模块完成详细设计、测试、实现和验证；一个模块未通过测试前不得同时铺开下一个模块。
- 每完成一个可运行、经过验证的版本，提交前必须检查完整差异并运行当前全部编译、测试和格式检查。
- 每个已完成版本必须提交到当前 Git 仓库并推送到 `origin` 当前分支。推送失败时必须报告本地提交号和失败原因。
- 不得提交生成报告、临时缓存、个人工具或 KiHan 工作副本中的文件。
- 最终验收必须为 khengine 中每一种实际语义写法建立对应的 1:1 最小复刻测试，并比较真实代码与测试代码的分析结果。
- 真实代码标签与语义分析不同时，必须由 AI 逐项判断是分析器缺陷、可信豁免、缺少 Reason 还是可补标 NLT，不得把源码标签直接当作测试真值。
- 性能验收必须分别运行 `-j1`、`-j2`、`-j4`，记录实际耗时和加速效果，并验证结果完全一致。

## 编码规范

- 代码遵循 `C:/Users/doucechen/Desktop/Personal/我维护的/规范/csharp编码规范/README.md`。
- 类型、方法、属性、命名空间和目录使用 PascalCase；局部变量和参数使用 camelCase；实例字段使用 `m_`；静态字段使用 `s_`。
- 使用 Allman 大括号、4 空格缩进、UTF-8 无 BOM，并为公开类型和方法编写 XML 文档注释。
- 禁止无意义缩写、布尔参数驱动多种行为、超大职责类和仅转发调用的空抽象。

## SHA-256 限制

- 禁止新增、实现或改写任何 SHA-256、SHA256、sha256、HMAC-SHA256 相关代码或封装。
- 可以使用、检查、说明或移除已有 SHA-256 库、API、方法、工具和命令。
